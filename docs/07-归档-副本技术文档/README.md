# 归档说明

本目录 Markdown 由原 **`副本/技术文档/`** 迁入，与主仓库 **`backend/`、`frontend/`、`database/`** 并行存在；文中若仍出现 `副本/数据库`、`副本/后端` 等路径，指 **`D:/HrDataCenter/副本/`** 下快照（未随 Markdown 一并移动），执行清单时请按你本机是否保留副本目录自行对照。

主仓库的正式脚本以根目录 **`database/`** 为准；与 `副本/数据库` 重复时优先以 **`database/`** 为源。
