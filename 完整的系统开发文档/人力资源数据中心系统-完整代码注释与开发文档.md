# 人力资源数据中心系统 - 完整代码注释与开发文档

## 📋 文档信息

| 项目名称 | HrDataCenter（人力资源数据中心系统） |
|---------|-----------------------------------|
| 项目类型 | 前后端分离的企业级应用系统 |
| 开发团队 | HrDataCenter开发团队 |
| 文档版本 | v1.0.0 |
| 创建日期 | 2026-03-03 |
| 文档类型 | 完整代码注释与开发文档 |

---

## 📖 目录

1. [项目概述](#1-项目概述)
2. [技术架构](#2-技术架构)
3. [项目结构](#3-项目结构)
4. [核心功能模块](#4-核心功能模块)
5. [代码详细注释](#5-代码详细注释)
6. [数据库设计](#6-数据库设计)
7. [API接口文档](#7-api接口文档)
8. [部署指南](#8-部署指南)
9. [开发规范](#9-开发规范)

---

## 1. 项目概述

### 1.1 项目简介

**人力资源数据中心系统**是一个基于Spring Boot + Vue 3的前后端分离企业级应用，旨在为企业提供全面的人力资源数据管理和分析服务。系统集成了MySQL和Hive双数据源，支持业务数据的实时管理和大数据分析的深度挖掘。

### 1.2 核心特性

- ✅ **用户认证与授权**：基于JWT的无状态认证，支持多角色权限管理
- ✅ **员工档案管理**：完整的员工信息CRUD，支持批量导入导出
- ✅ **数据分类管理**：数据目录和元数据管理，便于数据检索
- ✅ **数据同步管理**：MySQL到Hive的自动化数据同步
- ✅ **数据分析功能**：薪酬福利、员工流失、绩效等多维度分析
- ✅ **报表模板管理**：自定义报表模板，灵活生成各类报表
- ✅ **预警规则管理**：数据异常预警，及时发现问题
- ✅ **收藏夹功能**：常用数据收藏，提升工作效率
- ✅ **管理员功能**：用户管理、数据同步控制等高级功能

### 1.3 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Vue 3)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 用户界面 │  │ 数据展示 │  │ 图表分析 │  │ 报表管理 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↕ HTTP/RESTful API
┌─────────────────────────────────────────────────────────────┐
│                    控制器层 (Controller)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 认证控制 │  │ 数据控制 │  │ 分析控制 │  │ 管理控制 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    服务层 (Service)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 用户服务 │  │ 数据服务 │  │ 分析服务 │  │ 同步服务 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    数据访问层 (Mapper)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ UserMapper│ │ DataMapper│ │ HiveMapper│ │ 等其他    │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层 (Database)                     │
│  ┌──────────────────────┐  ┌──────────────────────┐       │
│  │   MySQL 8.0          │  │   Hive 3.1.3         │       │
│  │   (业务数据)          │  │   (大数据分析)       │       │
│  └──────────────────────┘  └──────────────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 技术架构

### 2.1 后端技术栈

| 技术组件 | 版本 | 说明 |
|---------|------|------|
| Spring Boot | 2.7.18 | 核心框架，提供自动配置和快速开发能力 |
| Spring Security | 5.7.11 | 安全框架，提供认证和授权功能 |
| MyBatis Plus | 3.5.5 | ORM框架，简化数据库操作 |
| MySQL | 8.0.33 | 关系型数据库，存储业务数据 |
| Hive | 3.1.3 | 数据仓库，用于大数据分析 |
| JWT | 0.11.5 | JSON Web Token，用于无状态认证 |
| Druid | 1.2.20 | 数据库连接池 |
| EasyExcel | 3.3.2 | Excel导入导出工具 |
| Lombok | 1.18.30 | 简化Java代码 |
| SLF4J + Logback | 1.7.36 | 日志框架 |

### 2.2 前端技术栈

| 技术组件 | 版本 | 说明 |
|---------|------|------|
| Vue | 3.4.0 | 渐进式JavaScript框架 |
| Vite | 5.0.0 | 前端构建工具 |
| Element Plus | 2.5.0 | Vue 3组件库 |
| Axios | 1.6.5 | HTTP客户端 |
| Vue Router | 4.2.5 | 路由管理 |
| Pinia | 2.1.7 | 状态管理 |
| ECharts | 5.4.3 | 数据可视化库 |

### 2.3 开发工具

- **IDE**：IntelliJ IDEA 2023.2
- **JDK**：1.8.0_381
- **Node.js**：推荐v18+
- **Maven**：3.8+
- **Git**：版本控制

---

## 3. 项目结构

### 3.1 后端项目结构

```
backend/
├── src/main/java/com/hr/backend/
│   ├── HrDataCenterApplication.java          # 启动类
│   ├── common/                               # 通用组件
│   │   ├── BaseException.java                # 自定义异常基类
│   │   ├── ErrorCode.java                    # 错误码枚举
│   │   └── Response.java                     # 统一响应结果
│   ├── config/                               # 配置类
│   │   ├── CorsConfig.java                   # 跨域配置
│   │   ├── PasswordEncoderConfig.java        # 密码编码器配置
│   │   ├── SecurityConfig.java               # Spring Security配置
│   │   └── TomcatConfig.java                 # Tomcat配置
│   ├── controller/                           # 控制器层
│   │   ├── admin/                            # 管理员控制器
│   │   │   ├── DataSyncController.java       # 数据同步控制器
│   │   │   └── UserAdminController.java      # 用户管理控制器
│   │   ├── AnalysisController.java           # 数据分析控制器
│   │   ├── AuthController.java               # 认证控制器
│   │   ├── DashboardController.java          # 仪表盘控制器
│   │   ├── DataCategoryController.java       # 数据分类控制器
│   │   ├── DataController.java               # 数据控制器
│   │   ├── FavoriteController.java           # 收藏夹控制器
│   │   ├── ReportController.java             # 报表控制器
│   │   └── RuleController.java               # 预警规则控制器
│   ├── exception/                            # 异常处理
│   │   └── GlobalExceptionHandler.java       # 全局异常处理器
│   ├── mapper/                               # 数据访问层
│   │   ├── DataCategoryMapper.java           # 数据分类Mapper
│   │   ├── DataSyncLogMapper.java            # 数据同步日志Mapper
│   │   ├── DepartmentMapper.java             # 部门Mapper
│   │   ├── EmployeeProfileMapper.java        # 员工档案Mapper
│   │   ├── FavoriteMapper.java               # 收藏夹Mapper
│   │   ├── ReportTemplateMapper.java         # 报表模板Mapper
│   │   ├── UserMapper.java                   # 用户Mapper
│   │   └── WarningRuleMapper.java            # 预警规则Mapper
│   ├── model/                                # 模型层
│   │   ├── dto/                              # 数据传输对象
│   │   │   ├── ChangePasswordRequest.java    # 修改密码请求
│   │   │   ├── LoginRequest.java             # 登录请求
│   │   │   ├── LoginResponse.java            # 登录响应
│   │   │   └── RegisterRequest.java          # 注册请求
│   │   └── entity/                           # 实体类
│   │       ├── DataCategory.java             # 数据分类实体
│   │       ├── DataSyncLog.java              # 数据同步日志实体
│   │       ├── Department.java               # 部门实体
│   │       ├── EmployeeProfile.java          # 员工档案实体
│   │       ├── Favorite.java                 # 收藏夹实体
│   │       ├── ReportTemplate.java           # 报表模板实体
│   │       ├── User.java                     # 用户实体
│   │       └── WarningRule.java              # 预警规则实体
│   ├── security/                             # 安全模块
│   │   ├── JwtAuthenticationFilter.java      # JWT认证过滤器
│   │   ├── JwtUtil.java                      # JWT工具类
│   │   └── SecurityUtil.java                 # 安全工具类
│   └── service/                              # 服务层
│       ├── AnalysisService.java              # 数据分析服务接口
│       ├── DashboardService.java             # 仪表盘服务接口
│       ├── DataCategoryService.java          # 数据分类服务接口
│       ├── DataSyncService.java              # 数据同步服务接口
│       ├── EmployeeProfileService.java       # 员工档案服务接口
│       ├── FavoriteService.java              # 收藏夹服务接口
│       ├── ReportTemplateService.java        # 报表模板服务接口
│       ├── UserService.java                  # 用户服务接口
│       ├── WarningRuleService.java           # 预警规则服务接口
│       └── impl/                             # 服务实现类
│           ├── AnalysisServiceImpl.java      # 数据分析服务实现
│           ├── DashboardServiceImpl.java     # 仪表盘服务实现
│           ├── DataCategoryServiceImpl.java   # 数据分类服务实现
│           ├── DataSyncServiceImpl.java      # 数据同步服务实现
│           ├── EmployeeProfileServiceImpl.java # 员工档案服务实现
│           ├── FavoriteServiceImpl.java      # 收藏夹服务实现
│           ├── ReportTemplateServiceImpl.java # 报表模板服务实现
│           ├── UserServiceImpl.java          # 用户服务实现
│           └── WarningRuleServiceImpl.java   # 预警规则服务实现
├── src/main/resources/
│   ├── mapper/                               # MyBatis映射文件
│   │   ├── EmployeeProfileMapper.xml         # 员工档案映射
│   │   └── UserMapper.xml                    # 用户映射
│   ├── application.yml                       # 主配置文件
│   ├── application-dev.yml                   # 开发环境配置
│   └── application-prod.yml                  # 生产环境配置
└── pom.xml                                   # Maven配置文件
```

### 3.2 前端项目结构

```
frontend/
├── public/
│   └── index.html                            # HTML入口
├── src/
│   ├── api/                                  # API接口封装
│   │   ├── analysis.js                       # 数据分析接口
│   │   ├── auth.js                           # 认证接口
│   │   ├── dashboard.js                      # 仪表盘接口
│   │   ├── data.js                           # 数据接口
│   │   ├── employee.js                       # 员工接口
│   │   ├── report.js                         # 报表接口
│   │   └── rule.js                           # 预警规则接口
│   ├── assets/                               # 静态资源
│   ├── components/                           # 公共组件
│   ├── router/                               # 路由配置
│   │   └── index.js                          # 路由定义
│   ├── stores/                               # 状态管理
│   │   └── user.js                           # 用户状态
│   ├── views/                                # 页面视图
│   │   ├── analysis/                         # 分析页面
│   │   │   ├── CompensationBenefits.vue      # 薪酬福利分析
│   │   │   ├── EmployeeTurnover.vue          # 员工流失分析
│   │   │   ├── PerformanceManagement.vue     # 绩效管理分析
│   │   │   ├── RecruitmentAnalysis.vue       # 招聘分析
│   │   │   └── TrainingDevelopment.vue       # 培训发展分析
│   │   ├── common/                           # 通用页面
│   │   │   ├── Dashboard.vue                 # 仪表盘
│   │   │   ├── EmployeeList.vue              # 员工列表
│   │   │   ├── EmployeeProfile.vue           # 员工档案
│   │   │   ├── Login.vue                     # 登录页
│   │   │   └── Register.vue                  # 注册页
│   │   └── admin/                            # 管理员页面
│   │       ├── DataManagement.vue            # 数据管理
│   │       ├── DataSync.vue                  # 数据同步
│   │       ├── ReportTemplate.vue            # 报表模板
│   │       ├── RuleManagement.vue            # 预警规则
│   │       └── UserManagement.vue            # 用户管理
│   ├── App.vue                               # 根组件
│   └── main.js                               # 应用入口
├── index.html                                # HTML模板
├── package.json                              # 依赖配置
├── vite.config.js                            # Vite配置
└── README.md                                 # 项目说明
```

---

## 4. 核心功能模块

### 4.1 用户认证与授权模块

**功能描述**：
- 用户注册：支持工号、姓名、密码、角色等信息注册
- 用户登录：基于JWT的无状态认证
- 密码管理：支持修改密码
- 权限控制：基于Spring Security的角色权限管理

**技术实现**：
- 使用BCrypt算法加密密码
- JWT Token有效期2小时
- 支持多角色：普通用户、管理员

**关键类**：
- `AuthController`：认证控制器
- `UserService`：用户服务
- `JwtUtil`：JWT工具类
- `JwtAuthenticationFilter`：JWT认证过滤器
- `SecurityConfig`：安全配置

### 4.2 员工档案管理模块

**功能描述**：
- 员工信息CRUD：增删改查员工档案
- 批量导入：支持Excel批量导入员工数据
- 批量导出：支持Excel批量导出员工数据
- 数据分类：按部门、职位等分类管理

**技术实现**：
- 使用EasyExcel处理Excel文件
- MyBatis Plus提供CRUD操作
- 支持分页查询

**关键类**：
- `DataController`：数据控制器
- `EmployeeProfileService`：员工档案服务
- `EmployeeProfileMapper`：员工档案Mapper

### 4.3 数据同步模块

**功能描述**：
- MySQL到Hive的数据同步
- 支持手动触发同步
- 记录同步日志
- 监控同步状态

**技术实现**：
- 使用JDBC连接Hive
- 定时任务或手动触发
- 同步日志记录

**关键类**：
- `DataSyncController`：数据同步控制器
- `DataSyncService`：数据同步服务
- `DataSyncLog`：同步日志实体

### 4.4 数据分析模块

**功能描述**：
- 薪酬福利分析：薪酬分布、福利统计
- 员工流失分析：流失率、流失原因
- 绩效管理分析：绩效分布、绩效趋势
- 招聘分析：招聘效率、招聘成本
- 培训发展分析：培训效果、技能提升

**技术实现**：
- 使用Hive进行大数据分析
- ECharts进行数据可视化
- 支持多维度分析

**关键类**：
- `AnalysisController`：数据分析控制器
- `AnalysisService`：数据分析服务

### 4.5 仪表盘模块

**功能描述**：
- 数据统计：员工总数、部门数量等
- 预警信息：数据异常预警
- 快捷入口：常用功能入口

**技术实现**：
- 聚合查询
- 实时更新

**关键类**：
- `DashboardController`：仪表盘控制器
- `DashboardService`：仪表盘服务

### 4.6 报表模板模块

**功能描述**：
- 报表模板管理：创建、编辑、删除报表模板
- 自定义报表：根据模板生成报表
- 报表导出：支持导出为Excel、PDF等格式

**技术实现**：
- 模板引擎
- 动态SQL

**关键类**：
- `ReportController`：报表控制器
- `ReportTemplateService`：报表模板服务

### 4.7 预警规则模块

**功能描述**：
- 预警规则管理：创建、编辑、删除预警规则
- 规则触发：根据规则自动触发预警
- 预警通知：发送预警通知

**技术实现**：
- 规则引擎
- 定时任务

**关键类**：
- `RuleController`：预警规则控制器
- `WarningRuleService`：预警规则服务

### 4.8 收藏夹模块

**功能描述**：
- 数据收藏：收藏常用的数据
- 收藏管理：查看、删除收藏
- 快速访问：快速访问收藏的数据

**技术实现**：
- 用户关联
- 数据引用

**关键类**：
- `FavoriteController`：收藏夹控制器
- `FavoriteService`：收藏夹服务

---

## 5. 代码详细注释

### 5.1 启动类

#### HrDataCenterApplication.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - Spring Boot 应用程序启动类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 项目描述：基于Spring Boot + Vue 3的前后端分离系统，用于企业人力资源数据管理和分析
 *
 * 技术栈：
 * - 后端框架：Spring Boot 2.7.18
 * - 持久层：MyBatis Plus 3.5.5
 * - 安全框架：Spring Security + JWT
 * - 数据库：MySQL 8.0（业务数据）+ Hive 3.1.3（大数据分析）
 * - 前端框架：Vue 3.4.0 + Element Plus
 *
 * 核心功能：
 * 1. 用户认证与授权（登录、注册、权限管理）
 * 2. 员工档案管理（增删改查、批量导入导出）
 * 3. 数据分类管理（数据目录、元数据管理）
 * 4. 数据同步管理（MySQL到Hive的数据同步）
 * 5. 数据分析功能（薪酬福利、员工流失、绩效管理等）
 * 6. 报表模板管理（自定义报表模板）
 * 7. 预警规则管理（数据异常预警）
 * 8. 收藏夹功能（常用数据收藏）
 * 9. 管理员功能（用户管理、数据同步控制）
 *
 * 系统架构：
 * - 采用标准的分层架构：Controller → Service → Mapper → Database
 * - 使用JWT进行无状态认证
 * - 支持MySQL和Hive双数据源
 * - 提供RESTful API接口
 *
 * 作者：HrDataCenter开发团队
 * 版本：1.0.0
 * ================================================================================
 */
package com.hr.backend;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring Boot应用程序启动类
 * <p>
 * 功能说明：
 * 1. 作为整个Spring Boot应用的入口点
 * 2. 启用Spring Boot自动配置
 * 3. 扫描并注册所有Spring组件（Controller、Service、Component等）
 * 4. 配置MyBatis Mapper接口扫描路径
 *
 * @MapperScan注解说明：
 * - 指定Mapper接口的扫描包路径为"com.hr.backend.mapper"
 * - Spring会自动扫描该包下的所有Mapper接口，并将其注册为Bean
 * - MyBatis Plus会自动为这些Mapper接口生成实现类
 */
@SpringBootApplication
@MapperScan("com.hr.backend.mapper")
public class HrDataCenterApplication {

    /**
     * 应用程序主入口方法
     * <p>
     * 执行流程：
     * 1. 初始化Spring Application上下文
     * 2. 加载所有配置文件（application.yml等）
     * 3. 扫描并注册所有Bean
     * 4. 启动内嵌的Tomcat服务器（默认端口8081）
     * 5. 应用启动完成，开始接收HTTP请求
     *
     * @param args 命令行参数，可用于覆盖配置文件中的配置项
     */
    public static void main(String[] args) {
        SpringApplication.run(HrDataCenterApplication.class, args);
    }
}
```

### 5.2 通用组件模块

#### 5.2.1 BaseException.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - 自定义业务异常类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：通用组件模块（common）
 *
 * 类说明：BaseException
 * 本类是系统中所有业务异常的基类，继承自RuntimeException
 * 用于封装业务逻辑中的异常情况，提供统一的异常处理机制
 *
 * 设计目的：
 * 1. 区分业务异常和系统异常，提供更精确的错误信息
 * 2. 支持自定义错误码和错误消息
 * 3. 配合GlobalExceptionHandler实现统一异常处理
 * 4. 便于前端根据错误码进行相应的提示和处理
 *
 * 使用场景：
 * - 用户名或密码错误
 * - 用户已存在
 * - 参数校验失败
 * - 权限不足
 * - 资源不存在
 * 等业务场景中的异常情况
 * ================================================================================
 */
package com.hr.backend.common;

import lombok.Getter;

/**
 * 自定义业务异常基类
 * <p>
 * 继承自RuntimeException，表示这是一个运行时异常
 * Spring的@Transactional注解默认只对RuntimeException进行回滚
 *
 * 功能特性：
 * 1. 封装错误码（code）和错误消息（message）
 * 2. 提供两种构造方式：使用ErrorCode枚举或直接指定码和消息
 * 3. 使用Lombok的@Getter注解自动生成getter方法
 *
 * 异常处理流程：
 * 1. 业务代码抛出BaseException
 * 2. GlobalExceptionHandler捕获该异常
 * 3. 封装为统一的Response对象返回给前端
 * 4. 前端根据错误码进行相应的UI提示
 */
@Getter
public class BaseException extends RuntimeException {

    /**
     * 错误码
     * <p>
     * 说明：
     * - 使用HTTP状态码风格的错误码
     * - 200：成功
     * - 400：参数错误
     * - 401：未授权
     * - 403：权限不足
     * - 404：资源不存在
     * - 409：冲突（如用户已存在）
     * - 500：系统错误
     */
    private final int code;

    /**
     * 构造方法1：使用ErrorCode枚举创建异常
     * <p>
     * 使用场景：
     * - 使用预定义的标准错误码和错误消息
     * - 保持错误信息的一致性
     *
     * @param ErrorCode 错误码枚举对象，包含code和message
     */
    public BaseException(ErrorCode errorCode) {
        super(errorCode.getMessage());
        this.code = errorCode.getCode();
    }

    /**
     * 构造方法2：使用自定义错误码和消息创建异常
     * <p>
     * 使用场景：
     * - 需要自定义错误消息（如具体的业务提示）
     * - 错误码需要动态确定
     *
     * @param code    错误码，建议使用HTTP状态码风格
     * @param message 错误消息，描述具体的错误原因
     */
    public BaseException(int code, String message) {
        super(message);
        this.code = code;
    }
}
```

#### 5.2.2 ErrorCode.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - 错误码枚举类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：通用组件模块（common）
 *
 * 类说明：ErrorCode
 * 本类定义了系统中所有标准错误码，采用枚举类型实现
 * 用于统一管理系统中的错误码和错误消息，避免魔法值的使用
 *
 * 设计目的：
 * 1. 集中管理所有错误码，便于维护和查找
 * 2. 提供类型安全的错误码定义，避免拼写错误
 * 3. 配合BaseException使用，提供标准的错误信息
 * 4. 使用HTTP状态码风格，符合RESTful规范
 *
 * 错误码设计原则：
 * - 2xx：成功
 * - 4xx：客户端错误（参数错误、未授权、权限不足等）
 * - 5xx：服务器错误（系统错误）
 * ================================================================================
 */
package com.hr.backend.common;

import lombok.Getter;

/**
 * 错误码枚举
 * <p>
 * 定义了系统中所有标准错误码和对应的错误消息
 * 使用Lombok的@Getter注解自动生成getter方法
 */
@Getter
public enum ErrorCode {

    /**
     * 成功
     * <p>
     * 使用场景：操作成功完成
     */
    SUCCESS(200, "成功"),

    /**
     * 参数错误
     * <p>
     * 使用场景：
     * - 请求参数缺失
     * - 参数格式不正确
     * - 参数校验失败
     */
    PARAM_ERROR(400, "参数错误"),

    /**
     * 未授权
     * <p>
     * 使用场景：
     * - 用户未登录
     * - Token无效或过期
     * - 用户名或密码错误
     */
    UNAUTHORIZED(401, "未授权"),

    /**
     * 权限不足
     * <p>
     * 使用场景：
     * - 用户已登录但无权访问该资源
     * - 角色权限不足
     */
    FORBIDDEN(403, "权限不足"),

    /**
     * 资源不存在
     * <p>
     * 使用场景：
     * - 查询的用户不存在
     * - 请求的接口不存在
     * - 访问的数据不存在
     */
    NOT_FOUND(404, "资源不存在"),

    /**
     * 用户已存在
     * <p>
     * 使用场景：
     * - 注册时用户名已存在
     * - 创建重复资源
     */
    USER_EXISTS(409, "用户已存在"),

    /**
     * 系统错误
     * <p>
     * 使用场景：
     * - 数据库连接失败
     * - 网络异常
     * - 服务器内部错误
     */
    SYSTEM_ERROR(500, "系统错误");

    /**
     * 错误码
     * <p>
     * 使用HTTP状态码风格，便于理解和使用
     */
    private final int code;

    /**
     * 错误消息
     * <p>
     * 描述错误的具体原因，用于展示给用户
     */
    private final String message;

    /**
     * 构造方法
     *
     * @param code    错误码
     * @param message 错误消息
     */
    ErrorCode(int code, String message) {
        this.code = code;
        this.message = message;
    }
}
```

#### 5.2.3 Response.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - 统一响应结果类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：通用组件模块（common）
 *
 * 类说明：Response
 * 本类是系统中所有API接口的统一响应结果封装类
 * 采用泛型设计，支持返回任意类型的数据
 *
 * 设计目的：
 * 1. 统一API响应格式，便于前端处理
 * 2. 封装响应码、响应消息和响应数据
 * 3. 提供成功和失败的快速构建方法
 * 4. 支持泛型，灵活适应各种业务场景
 *
 * 响应格式：
 * {
 *   "code": 200,           // 响应码
 *   "message": "success",  // 响应消息
 *   "data": {...}          // 响应数据（可能为null）
 * }
 * ================================================================================
 */
package com.hr.backend.common;

import lombok.Data;

/**
 * 统一响应结果类
 * <p>
 * 使用泛型T支持返回任意类型的数据
 * 使用Lombok的@Data注解自动生成getter/setter方法
 *
 * 字段说明：
 * - code：响应码，200表示成功，其他表示失败
 * - message：响应消息，描述操作结果
 * - data：响应数据，泛型类型，可能为null
 */
@Data
public class Response<T> {

    /**
     * 响应码
     * <p>
     * 标准：
     * - 200：成功
     * - 400：参数错误
     * - 401：未授权
     * - 403：权限不足
     * - 404：资源不存在
     * - 500：系统错误
     */
    private int code;

    /**
     * 响应消息
     * <p>
     * 描述操作结果，如"success"、"参数错误"等
     */
    private String message;

    /**
     * 响应数据
     * <p>
     * 泛型类型，可以是任意业务数据
     * 如果不需要返回数据，则为null
     */
    private T data;

    /**
     * 构建成功响应（无数据）
     * <p>
     * 使用场景：
     * - 删除操作
     * - 更新操作
     * - 不需要返回数据的操作
     *
     * @param <T> 数据类型
     * @return 成功响应对象，data为null
     */
    public static <T> Response<T> success() {
        return success(null);
    }

    /**
     * 构建成功响应（带数据）
     * <p>
     * 使用场景：
     * - 查询操作
     * - 登录操作（返回token）
     * - 需要返回数据的操作
     *
     * @param data 响应数据
     * @param <T>  数据类型
     * @return 成功响应对象，包含指定数据
     */
    public static <T> Response<T> success(T data) {
        Response<T> r = new Response<>();
        r.setCode(200);
        r.setMessage("success");
        r.setData(data);
        return r;
    }

    /**
     * 构建错误响应
     * <p>
     * 使用场景：
     * - 业务异常
     * - 系统异常
     * - 参数校验失败
     *
     * @param code    错误码
     * @param message 错误消息
     * @param <T>     数据类型
     * @return 错误响应对象，data为null
     */
    public static <T> Response<T> error(int code, String message) {
        Response<T> r = new Response<>();
        r.setCode(code);
        r.setMessage(message);
        r.setData(null);
        return r;
    }
}
```

### 5.3 配置模块

#### 5.3.1 CorsConfig.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - 跨域配置类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：配置模块（config）
 *
 * 类说明：CorsConfig
 * 本类用于配置跨域资源共享（CORS）策略，解决前后端分离时的跨域问题
 *
 * 设计目的：
 * 1. 允许前端应用（运行在不同端口或域名）访问后端API
 * 2. 配置允许的请求方法、请求头和来源
 * 3. 支持携带凭证（如Cookie、Authorization头）
 *
 * 跨域场景：
 * - 前端：http://localhost:5173（Vue开发服务器）
 * - 后端：http://localhost:8081（Spring Boot服务器）
 * - 生产环境：可能部署在不同域名下
 * ================================================================================
 */
package com.hr.backend.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.UrlBasedCorsConfigurationSource;
import org.springframework.web.filter.CorsFilter;

/**
 * 跨域配置类
 * <p>
 * 使用@Configuration注解标记为配置类
 * 创建CorsFilter Bean来处理跨域请求
 */
@Configuration
public class CorsConfig {

    /**
     * 创建CORS过滤器Bean
     * <p>
     * 配置说明：
     * - 允许所有来源（Origin）：使用addAllowedOriginPattern("*")支持动态origin
     * - 允许所有请求头：addAllowedHeader("*")
     * - 允许所有HTTP方法：addAllowedMethod("*")（GET、POST、PUT、DELETE等）
     * - 允许携带凭证：setAllowCredentials(true)（如Cookie、JWT Token等）
     * - 应用到所有路径：/** 表示匹配所有请求路径
     *
     * 注意事项：
     * - 当设置allowCredentials为true时，不能使用addAllowedOrigin("*")
     * - 必须使用addAllowedOriginPattern("*")或指定具体的origin
     * - 生产环境建议限制具体的origin，提高安全性
     *
     * @return CORS过滤器实例
     */
    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        // 允许所有来源（支持动态origin，如localhost的不同端口）
        config.addAllowedOriginPattern("*");
        // 允许所有请求头
        config.addAllowedHeader("*");
        // 允许所有HTTP方法（GET、POST、PUT、DELETE、OPTIONS等）
        config.addAllowedMethod("*");
        // 允许携带凭证（Cookie、Authorization头等）
        config.setAllowCredentials(true);

        // 创建CORS配置源，应用到所有路径
        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);
        return new CorsFilter(source);
    }
}
```

#### 5.3.2 PasswordEncoderConfig.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - 密码编码器配置类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：配置模块（config）
 *
 * 类说明：PasswordEncoderConfig
 * 本类用于配置Spring Security的密码编码器，用于用户密码的加密和校验
 *
 * 设计目的：
 * 1. 提供安全的密码加密存储方案
 * 2. 使用BCrypt算法进行密码加密
 * 3. 避免循环依赖问题（将PasswordEncoder Bean从SecurityConfig中分离）
 *
 * BCrypt算法特点：
 * - 单向加密：无法解密，只能通过加密后比较
 * - 自动加盐：每次加密结果都不同，防止彩虹表攻击
 * - 可调节强度：默认强度为10，可根据需要调整
 * - 安全性高：适合密码存储场景
 * ================================================================================
 */
package com.hr.backend.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;

/**
 * 密码编码器配置类
 * <p>
 * 使用@Configuration注解标记为配置类
 * 创建PasswordEncoder Bean供整个应用使用
 */
@Configuration
public class PasswordEncoderConfig {

    /**
     * 创建密码编码器Bean
     * <p>
     * 使用BCryptPasswordEncoder实现：
     * - BCrypt是一种基于Blowfish的密码哈希算法
     * - 每次加密都会自动生成随机盐值
     * - 相同的明文密码每次加密结果都不同
     * - 但可以通过matches方法正确校验密码
     *
     * 使用场景：
     * 1. 用户注册时：使用encode()方法加密密码后存储
     * 2. 用户登录时：使用matches()方法比对明文密码和加密后的密码
     *
     * 示例：
     * // 加密
     * String encodedPassword = passwordEncoder.encode("123456");
     * // 校验
     * boolean matches = passwordEncoder.matches("123456", encodedPassword);
     *
     * @return BCrypt密码编码器实例
     */
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

#### 5.3.3 SecurityConfig.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - Spring Security安全配置类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：配置模块（config）
 *
 * 类说明：SecurityConfig
 * 本类用于配置Spring Security的安全策略，包括认证和授权规则
 *
 * 设计目的：
 * 1. 配置基于JWT的无状态认证
 * 2. 配置接口访问权限控制
 * 3. 配置异常处理（未授权、权限不足等）
 * 4. 集成JWT认证过滤器
 *
 * 安全策略：
 * - 禁用CSRF保护（使用JWT，不需要CSRF）
 * - 使用无状态Session（STATELESS）
 * - 登录注册接口允许匿名访问
 * - 其他API接口需要认证
 * - 启用方法级权限控制
 * ================================================================================
 */
package com.hr.backend.config;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.hr.backend.common.Response;
import com.hr.backend.security.JwtAuthenticationFilter;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.method.configuration.EnableGlobalMethodSecurity;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.annotation.web.configuration.WebSecurityConfigurerAdapter;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletResponse;

/**
 * Spring Security配置类
 * <p>
 * 继承WebSecurityConfigurerAdapter进行自定义配置
 * 使用@EnableWebSecurity启用Web安全功能
 * 使用@EnableGlobalMethodSecurity启用方法级权限控制
 */
@Configuration
@EnableWebSecurity
@EnableGlobalMethodSecurity(prePostEnabled = true)
public class SecurityConfig extends WebSecurityConfigurerAdapter {

    /**
     * 注入JWT认证过滤器
     * <p>
     * 该过滤器会在UsernamePasswordAuthenticationFilter之前执行
     * 用于从请求头中提取JWT Token并进行认证
     */
    @Resource
    private JwtAuthenticationFilter jwtAuthenticationFilter;

    /**
     * 注入密码编码器
     * <p>
     * 从PasswordEncoderConfig Bean中注入
     * 用于用户密码的加密和校验
     */
    @Resource
    private PasswordEncoder passwordEncoder;

    /**
     * 配置HTTP安全策略
     * <p>
     * 配置内容：
     * 1. 禁用CSRF保护（JWT不需要）
     * 2. 设置Session为无状态（STATELESS）
     * 3. 配置接口访问权限
     * 4. 配置异常处理
     * 5. 添加JWT认证过滤器
     *
     * @param http HttpSecurity对象，用于配置HTTP安全
     * @throws Exception 配置异常
     */
    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http
                // 禁用CSRF保护（使用JWT，不需要CSRF）
                .csrf().disable()
                // 配置Session管理策略为无状态
                .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS)
                .and()
                // 配置请求授权
                .authorizeRequests()
                // 允许匿名访问的接口（登录、注册）
                .antMatchers("/api/auth/login", "/api/auth/register").permitAll()
                // 其他/api/**接口需要认证
                .antMatchers("/api/**").authenticated()
                // 其他请求允许访问
                .anyRequest().permitAll()
                .and()
                // 配置异常处理
                .exceptionHandling()
                // 认证失败处理（401）
                .authenticationEntryPoint((req, res, e) -> {
                    res.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
                    res.setContentType("application/json;charset=UTF-8");
                    res.getWriter().write(new ObjectMapper().writeValueAsString(Response.error(401, "未授权，请登录")));
                })
                .and()
                // 添加JWT认证过滤器，在UsernamePasswordAuthenticationFilter之前执行
                .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
    }
}
```

#### 5.3.4 TomcatConfig.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - Tomcat配置类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：配置模块（config）
 *
 * 类说明：TomcatConfig
 * 本类用于配置内嵌Tomcat服务器，解决Hive依赖与Tomcat的兼容性问题
 *
 * 设计目的：
 * 1. 禁用Tomcat的TLD（Tag Library Descriptor）扫描
 * 2. 避免Hive等依赖带入的老旧TldScanner与Spring Boot内置Tomcat 9冲突
 * 3. 解决AbstractMethodError问题
 *
 * 问题背景：
 * - Hive 3.1.3依赖中包含了一些老旧的JAR包
 * - 这些JAR包中的TldScanner与Spring Boot 2.7.18内置的Tomcat 9不兼容
 * - 导致启动时报AbstractMethodError
 *
 * 解决方案：
 * - 禁用Tomcat的TLD扫描和类路径扫描
 * - 不影响应用正常运行
 * ================================================================================
 */
package com.hr.backend.config;

import org.apache.catalina.Context;
import org.apache.tomcat.util.scan.StandardJarScanner;
import org.springframework.boot.web.embedded.tomcat.TomcatServletWebServerFactory;
import org.springframework.boot.web.server.WebServerFactoryCustomizer;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * Tomcat配置类
 * <p>
 * 使用@Configuration注解标记为配置类
 * 通过自定义TomcatServletWebServerFactory来禁用TLD扫描
 */
@Configuration
public class TomcatConfig {

    /**
     * 创建Tomcat自定义器Bean
     * <p>
     * 功能：
     * - 自定义Tomcat的配置
     * - 添加Context自定义器来禁用TLD扫描
     *
     * @return WebServerFactoryCustomizer实例
     */
    @Bean
    public WebServerFactoryCustomizer<TomcatServletWebServerFactory> tomcatCustomizer() {
        return factory -> factory.addContextCustomizers(this::disableTldScanning);
    }

    /**
     * 禁用TLD扫描
     * <p>
     * 功能：
     * - 获取Tomcat的JarScanner
     * - 如果是StandardJarScanner，则禁用Manifest和类路径扫描
     *
     * 为什么禁用：
     * - Hive等大数据依赖中包含老旧的JAR包
     * - 这些JAR包的TLD扫描器与Tomcat 9不兼容
     * - 禁用扫描可以避免AbstractMethodError
     *
     * @param context Tomcat Context对象
     */
    private void disableTldScanning(Context context) {
        if (context.getJarScanner() instanceof StandardJarScanner) {
            StandardJarScanner scanner = (StandardJarScanner) context.getJarScanner();
            // 禁用Manifest扫描
            scanner.setScanManifest(false);
            // 禁用类路径扫描
            scanner.setScanClassPath(false);
        }
    }
}
```

### 5.4 异常处理模块

#### GlobalExceptionHandler.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - 全局异常处理器
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：异常处理模块（exception）
 *
 * 类说明：GlobalExceptionHandler
 * 本类用于统一处理系统中的各种异常，将异常转换为统一的响应格式返回给前端
 *
 * 设计目的：
 * 1. 集中处理所有异常，避免在每个Controller中重复编写异常处理代码
 * 2. 提供统一的错误响应格式
 * 3. 记录异常日志，便于问题排查
 * 4. 提供友好的错误提示给用户
 *
 * 处理的异常类型：
 * - BaseException：业务异常
 * - RuntimeException：运行时异常
 * - MethodArgumentNotValidException：参数校验异常
 * - AuthenticationException：认证异常
 * - AccessDeniedException：权限异常
 * ================================================================================
 */
package com.hr.backend.exception;

import com.hr.backend.common.BaseException;
import com.hr.backend.common.ErrorCode;
import com.hr.backend.common.Response;
import lombok.extern.slf4j.Slf4j;
import org.springframework.security.access.AccessDeniedException;
import org.springframework.security.core.AuthenticationException;
import org.springframework.validation.BindingResult;
import org.springframework.validation.FieldError;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

import java.util.stream.Collectors;

/**
 * 全局异常处理器
 * <p>
 * 使用@RestControllerAdvice注解标记为全局异常处理器
 * 使用@Slf4j注解提供日志功能
 *
 * 工作原理：
 * - 当Controller中抛出异常时，Spring会自动调用对应的@ExceptionHandler方法
 * - 将异常转换为Response对象返回给前端
 * - 记录异常日志
 */
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    /**
     * 处理业务异常（BaseException）
     * <p>
     * 使用场景：
     * - 用户名或密码错误
     * - 用户已存在
     * - 参数校验失败
     * - 权限不足
     * 等业务场景中的异常
     *
     * @param e 业务异常对象
     * @return 错误响应对象
     */
    @ExceptionHandler(BaseException.class)
    public Response<Void> handleBaseException(BaseException e) {
        log.error("业务异常: {}", e.getMessage());
        return Response.error(e.getCode(), e.getMessage());
    }

    /**
     * 处理运行时异常（RuntimeException）
     * <p>
     * 使用场景：
     * - 数据库连接失败
     * - 网络异常
     * - 空指针异常
     * 等系统级异常
     *
     * @param e 运行时异常对象
     * @return 错误响应对象
     */
    @ExceptionHandler(RuntimeException.class)
    public Response<Void> handleRuntimeException(RuntimeException e) {
        log.error("运行时异常: {}", e.getMessage(), e);
        return Response.error(ErrorCode.SYSTEM_ERROR.getCode(), "系统错误，请稍后重试");
    }

    /**
     * 处理参数校验异常（MethodArgumentNotValidException）
     * <p>
     * 使用场景：
     * - 请求参数不符合@Validated校验规则
     * - 字段为空、格式不正确等
     *
     * @param e 参数校验异常对象
     * @return 错误响应对象
     */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Response<Void> handleValidationException(MethodArgumentNotValidException e) {
        BindingResult br = e.getBindingResult();
        // 提取所有字段错误消息，用"; "连接
        String msg = br.getFieldErrors().stream()
                .map(FieldError::getDefaultMessage)
                .collect(Collectors.joining("; "));
        log.error("参数校验异常: {}", msg);
        return Response.error(ErrorCode.PARAM_ERROR.getCode(), msg);
    }

    /**
     * 处理认证异常（AuthenticationException）
     * <p>
     * 使用场景：
     * - Token无效或过期
     * - 用户名或密码错误
     * - 认证失败
     *
     * @param e 认证异常对象
     * @return 错误响应对象
     */
    @ExceptionHandler(AuthenticationException.class)
    public Response<Void> handleAuthenticationException(AuthenticationException e) {
        log.error("认证异常: {}", e.getMessage());
        return Response.error(ErrorCode.UNAUTHORIZED.getCode(), "认证失败，请重新登录");
    }

    /**
     * 处理权限异常（AccessDeniedException）
     * <p>
     * 使用场景：
     * - 用户已登录但无权访问该资源
     * - 角色权限不足
     *
     * @param e 权限异常对象
     * @return 错误响应对象
     */
    @ExceptionHandler(AccessDeniedException.class)
    public Response<Void> handleAccessDeniedException(AccessDeniedException e) {
        log.error("权限异常: {}", e.getMessage());
        return Response.error(ErrorCode.FORBIDDEN.getCode(), "权限不足");
    }
}
```

### 5.5 安全模块

#### 5.5.1 JwtUtil.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - JWT工具类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：安全模块（security）
 *
 * 类说明：JwtUtil
 * 本类用于生成、解析和验证JWT Token
 *
 * 设计目的：
 * 1. 提供无状态的认证机制
 * 2. 支持Token的生成、解析和验证
 * 3. 封装JWT相关操作，简化使用
 *
 * JWT（JSON Web Token）简介：
 * - JWT是一种开放标准（RFC 7519）
 * - 用于在各方之间安全地传输信息
 * - 由三部分组成：Header、Payload、Signature
 * - 无状态，不需要在服务器端存储会话信息
 *
 * Token结构：
 * {
 *   "userId": 1,
 *   "username": "admin",
 *   "role": "ADMIN",
 *   "sub": "admin",
 *   "iat": 1709280000,
 *   "exp": 1709287200
 * }
 * ================================================================================
 */
package com.hr.backend.security;

import io.jsonwebtoken.*;
import io.jsonwebtoken.security.Keys;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import javax.crypto.SecretKey;
import java.nio.charset.StandardCharsets;
import java.util.Date;
import java.util.HashMap;
import java.util.Map;

/**
 * JWT工具类
 * <p>
 * 使用@Component注解标记为Spring组件
 * 使用@Slf4j注解提供日志功能
 */
@Component
@Slf4j
public class JwtUtil {

    /**
     * JWT密钥
     * <p>
     * 从配置文件中读取，用于签名和验证Token
     * 建议使用足够长的随机字符串
     */
    @Value("${jwt.secret}")
    private String secret;

    /**
     * Token过期时间（毫秒）
     * <p>
     * 从配置文件中读取，默认为2小时（7200000毫秒）
     */
    @Value("${jwt.expiration}")
    private Long expiration;

    /**
     * 获取签名密钥
     * <p>
     * 功能：
     * - 将配置的密钥字符串转换为SecretKey对象
     * - 使用HMAC-SHA算法
     *
     * @return SecretKey对象
     */
    private SecretKey getSigningKey() {
        byte[] keyBytes = secret.getBytes(StandardCharsets.UTF_8);
        return Keys.hmacShaKeyFor(keyBytes);
    }

    /**
     * 生成JWT Token
     * <p>
     * 功能：
     * - 将用户信息封装到Token中
     * - 设置Token的过期时间
     * - 使用密钥签名Token
     *
     * @param userId   用户ID
     * @param username 用户名
     * @param role     用户角色
     * @return JWT Token字符串
     */
    public String generateToken(Long userId, String username, String role) {
        Map<String, Object> claims = new HashMap<>();
        claims.put("userId", userId);
        claims.put("username", username);
        claims.put("role", role);
        Date now = new Date();
        Date expiry = new Date(now.getTime() + expiration);
        return Jwts.builder()
                .setClaims(claims)
                .setSubject(username)
                .setIssuedAt(now)
                .setExpiration(expiry)
                .signWith(getSigningKey(), SignatureAlgorithm.HS512)
                .compact();
    }

    /**
     * 从Token中获取用户名
     * <p>
     * 功能：
     * - 解析Token
     * - 提取Subject字段（即用户名）
     *
     * @param token JWT Token字符串
     * @return 用户名，解析失败返回null
     */
    public String getUsernameFromToken(String token) {
        try {
            return getClaimsFromToken(token).getSubject();
        } catch (Exception e) {
            log.error("从Token获取用户名失败", e);
            return null;
        }
    }

    /**
     * 从Token中获取用户ID
     * <p>
     * 功能：
     * - 解析Token
     * - 提取userId字段
     *
     * @param token JWT Token字符串
     * @return 用户ID，解析失败返回null
     */
    public Long getUserIdFromToken(String token) {
        try {
            Object v = getClaimsFromToken(token).get("userId");
            return v == null ? null : Long.valueOf(v.toString());
        } catch (Exception e) {
            log.error("从Token获取用户ID失败", e);
            return null;
        }
    }

    /**
     * 从Token中获取用户角色
     * <p>
     * 功能：
     * - 解析Token
     * - 提取role字段
     *
     * @param token JWT Token字符串
     * @return 用户角色，解析失败返回null
     */
    public String getRoleFromToken(String token) {
        try {
            Object v = getClaimsFromToken(token).get("role");
            return v == null ? null : v.toString();
        } catch (Exception e) {
            log.error("从Token获取角色失败", e);
            return null;
        }
    }

    /**
     * 验证Token是否有效
     * <p>
     * 功能：
     * - 验证Token签名
     * - 验证Token格式
     * - 验证Token是否过期
     *
     * @param token JWT Token字符串
     * @return true表示Token有效，false表示Token无效
     */
    public boolean validateToken(String token) {
        try {
            Jwts.parserBuilder()
                    .setSigningKey(getSigningKey())
                    .build()
                    .parseClaimsJws(token);
            return true;
        } catch (SecurityException e) {
            log.error("Token签名无效");
        } catch (MalformedJwtException e) {
            log.error("Token格式无效");
        } catch (ExpiredJwtException e) {
            log.error("Token已过期");
        } catch (Exception e) {
            log.error("Token无效", e);
        }
        return false;
    }

    /**
     * 从Token中获取Claims
     * <p>
     * 功能：
     * - 解析Token
     * - 返回Claims对象（包含Token中的所有信息）
     *
     * @param token JWT Token字符串
     * @return Claims对象
     */
    private Claims getClaimsFromToken(String token) {
        return Jwts.parserBuilder()
                .setSigningKey(getSigningKey())
                .build()
                .parseClaimsJws(token)
                .getBody();
    }
}
```

#### 5.5.2 JwtAuthenticationFilter.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - JWT认证过滤器
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：安全模块（security）
 *
 * 类说明：JwtAuthenticationFilter
 * 本类用于拦截HTTP请求，验证JWT Token，提取用户信息并设置到Security上下文中
 *
 * 设计目的：
 * 1. 实现基于JWT的无状态认证
 * 2. 从请求头中提取Token
 * 3. 验证Token有效性
 * 4. 将用户信息设置到Security上下文中
 *
 * 工作流程：
 * 1. 拦截每个HTTP请求
 * 2. 从请求头中提取Authorization字段的Bearer Token
 * 3. 验证Token有效性
 * 4. 从Token中提取用户信息
 * 5. 从数据库查询用户详细信息
 * 6. 将用户信息设置到Security上下文中
 * 7. 放行请求
 * ================================================================================
 */
package com.hr.backend.security;

import com.hr.backend.model.entity.User;
import com.hr.backend.service.UserService;
import lombok.extern.slf4j.Slf4j;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.security.web.authentication.WebAuthenticationDetailsSource;
import org.springframework.stereotype.Component;
import org.springframework.util.StringUtils;
import org.springframework.web.filter.OncePerRequestFilter;

import javax.annotation.Resource;
import javax.servlet.FilterChain;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;

/**
 * JWT认证过滤器
 * <p>
 * 继承OncePerRequestFilter，确保每个请求只执行一次
 * 使用@Component注解标记为Spring组件
 * 使用@Slf4j注解提供日志功能
 */
@Component
@Slf4j
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    /**
     * JWT工具类
     * <p>
     * 用于生成、解析和验证JWT Token
     */
    @Resource
    private JwtUtil jwtUtil;

    /**
     * 用户服务
     * <p>
     * 用于查询用户详细信息
     */
    @Resource
    private UserService userService;

    /**
     * 过滤器核心方法
     * <p>
     * 功能：
     * 1. 从请求中提取Token
     * 2. 验证Token有效性
     * 3. 提取用户信息并设置到Security上下文中
     *
     * @param request     HTTP请求对象
     * @param response    HTTP响应对象
     * @param filterChain 过滤器链
     * @throws ServletException Servlet异常
     * @throws IOException      IO异常
     */
    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain) throws ServletException, IOException {
        // 从请求中提取Token
        String token = getTokenFromRequest(request);

        // 如果Token存在且有效
        if (StringUtils.hasText(token) && jwtUtil.validateToken(token)) {
            // 从Token中提取用户信息
            String username = jwtUtil.getUsernameFromToken(token);
            Long userId = jwtUtil.getUserIdFromToken(token);

            // 从数据库查询用户详细信息
            User user = userService.getById(userId);

            // 如果用户存在且状态正常
            if (user != null && user.getStatus() != null && user.getStatus() == 1) {
                // 创建认证对象
                UsernamePasswordAuthenticationToken authentication =
                        new UsernamePasswordAuthenticationToken(user, null, user.getAuthorities());
                // 设置认证详情
                authentication.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
                // 将认证对象设置到Security上下文中
                SecurityContextHolder.getContext().setAuthentication(authentication);
            }
        }

        // 放行请求
        filterChain.doFilter(request, response);
    }

    /**
     * 从请求中提取Token
     * <p>
     * 功能：
     * - 从请求头的Authorization字段中提取Bearer Token
     * - Authorization格式：Bearer <token>
     *
     * @param request HTTP请求对象
     * @return JWT Token字符串，如果不存在返回null
     */
    private String getTokenFromRequest(HttpServletRequest request) {
        String bearer = request.getHeader("Authorization");
        if (bearer != null && bearer.startsWith("Bearer ")) {
            // 去除"Bearer "前缀，返回Token
            return bearer.substring(7);
        }
        return null;
    }
}
```

#### 5.5.3 SecurityUtil.java

```java
/**
 * ================================================================================
 * 人力资源数据中心系统 - 安全工具类
 * ================================================================================
 * 项目名称：HrDataCenter（人力资源数据中心系统）
 * 模块名称：安全模块（security）
 *
 * 类说明：SecurityUtil
 * 本类提供了一些便捷的安全相关工具方法
 *
 * 设计目的：
 * 1. 提供获取当前登录用户的方法
 * 2. 简化Security上下文的操作
 * ================================================================================
 */
package com.hr.backend.security;

import com.hr.backend.model.entity.User;
import org.springframework.security.core.context.SecurityContextHolder;

/**
 * 安全工具类
 * <p>
 * 提供静态方法，简化Security上下文的操作
 */
public class SecurityUtil {

    /**
     * 获取当前登录用户
     * <p>
     * 功能：
     * - 从Security上下文中获取认证对象
     * - 提取用户信息
     *
     * @return 当前登录用户对象，如果未登录返回null
     */
    public static User getCurrentUser() {
        try {
            Object principal = SecurityContextHolder.getContext().getAuthentication().getPrincipal();
            if (principal instanceof User) {
                return (User) principal;
            }
        } catch (Exception e) {
            // 异常情况返回null
        }
        return null;
    }

    /**
     * 获取当前登录用户ID
     * <p>
     * 功能：
     * - 获取当前登录用户
     * - 提取用户ID
     *
     * @return 当前登录用户ID，如果未登录返回null
     */
    public static Long getCurrentUserId() {
        User user = getCurrentUser();
        return user != null ? user.getId() : null;
    }

    /**
     * 获取当前登录用户名
     * <p>
     * 功能：
     * - 获取当前登录用户
     * - 提取用户名
     *
     * @return 当前登录用户名，如果未登录返回null
     */
    public static String getCurrentUsername() {
        User user = getCurrentUser();
        return user != null ? user.getUsername() : null;
    }

    /**
     * 获取当前登录用户角色
     * <p>
     * 功能：
     * - 获取当前登录用户
     * - 提取用户角色
     *
     * @return 当前登录用户角色，如果未登录返回null
     */
    public static String getCurrentRole() {
        User user = getCurrentUser();
        return user != null ? user.getRole() : null;
    }
}
```

由于文档篇幅限制，以上展示了部分核心代码的详细注释。完整的代码注释已在所有Java源文件中添加。

---

## 6. 数据库设计

### 6.1 MySQL数据库设计

#### 6.1.1 用户表（user）

| 字段名 | 类型 | 说明 | 约束 |
|-------|------|------|------|
| id | BIGINT | 用户ID | 主键，自增 |
| username | VARCHAR(50) | 用户名（工号） | 唯一，非空 |
| password | VARCHAR(255) | 密码（BCrypt加密） | 非空 |
| name | VARCHAR(50) | 姓名 | 非空 |
| role | VARCHAR(20) | 角色 | 非空（USER/ADMIN） |
| dept_id | BIGINT | 部门ID | 外键 |
| dept_name | VARCHAR(100) | 部门名称 | |
| dept_scope | VARCHAR(500) | 数据权限范围 | |
| phone | VARCHAR(20) | 电话 | |
| email | VARCHAR(100) | 邮箱 | |
| status | INT | 状态 | 1-正常，0-禁用 |
| is_deleted | INT | 逻辑删除 | 0-未删除，1-已删除 |
| create_time | DATETIME | 创建时间 | |
| update_time | DATETIME | 更新时间 | |

#### 6.1.2 员工档案表（employee_profile）

| 字段名 | 类型 | 说明 | 约束 |
|-------|------|------|------|
| id | BIGINT | 员工ID | 主键，自增 |
| employee_no | VARCHAR(50) | 员工工号 | 唯一，非空 |
| name | VARCHAR(50) | 姓名 | 非空 |
| gender | VARCHAR(10) | 性别 | |
| birth_date | DATE | 出生日期 | |
| id_card | VARCHAR(18) | 身份证号 | |
| phone | VARCHAR(20) | 电话 | |
| email | VARCHAR(100) | 邮箱 | |
| department_id | BIGINT | 部门ID | 外键 |
| department_name | VARCHAR(100) | 部门名称 | |
| position | VARCHAR(50) | 职位 | |
| level | VARCHAR(20) | 职级 | |
| hire_date | DATE | 入职日期 | |
| status | VARCHAR(20) | 在职状态 | 在职/离职/休假 |
| education | VARCHAR(20) | 学历 | |
| address | VARCHAR(200) | 地址 | |
| is_deleted | INT | 逻辑删除 | 0-未删除，1-已删除 |
| create_time | DATETIME | 创建时间 | |
| update_time | DATETIME | 更新时间 | |

#### 6.1.3 部门表（department）

| 字段名 | 类型 | 说明 | 约束 |
|-------|------|------|------|
| id | BIGINT | 部门ID | 主键，自增 |
| name | VARCHAR(100) | 部门名称 | 非空 |
| parent_id | BIGINT | 上级部门ID | |
| leader | VARCHAR(50) | 部门负责人 | |
| phone | VARCHAR(20) | 联系电话 | |
| description | VARCHAR(500) | 部门描述 | |
| is_deleted | INT | 逻辑删除 | 0-未删除，1-已删除 |
| create_time | DATETIME | 创建时间 | |
| update_time | DATETIME | 更新时间 | |

#### 6.1.4 数据分类表（data_category）

| 字段名 | 类型 | 说明 | 约束 |
|-------|------|------|------|
| id | BIGINT | 分类ID | 主键，自增 |
| name | VARCHAR(100) | 分类名称 | 非空 |
| code | VARCHAR(50) | 分类编码 | 唯一 |
| parent_id | BIGINT | 上级分类ID | |
| description | VARCHAR(500) | 分类描述 | |
| sort_order | INT | 排序 | |
| is_deleted | INT | 逻辑删除 | 0-未删除，1-已删除 |
| create_time | DATETIME | 创建时间 | |
| update_time | DATETIME | 更新时间 | |

#### 6.1.5 报表模板表（report_template）

| 字段名 | 类型 | 说明 | 约束 |
|-------|------|------|------|
| id | BIGINT | 模板ID | 主键，自增 |
| name | VARCHAR(100) | 模板名称 | 非空 |
| code | VARCHAR(50) | 模板编码 | 唯一 |
| type | VARCHAR(20) | 模板类型 | |
| content | TEXT | 模板内容 | |
| description | VARCHAR(500) | 模板描述 | |
| creator_id | BIGINT | 创建人ID | |
| is_deleted | INT | 逻辑删除 | 0-未删除，1-已删除 |
| create_time | DATETIME | 创建时间 | |
| update_time | DATETIME | 更新时间 | |

#### 6.1.6 预警规则表（warning_rule）

| 字段名 | 类型 | 说明 | 约束 |
|-------|------|------|------|
| id | BIGINT | 规则ID | 主键，自增 |
| name | VARCHAR(100) | 规则名称 | 非空 |
| rule_type | VARCHAR(20) | 规则类型 | |
| condition | TEXT | 触发条件 | |
| threshold | VARCHAR(50) | 阈值 | |
| level | VARCHAR(20) | 预警级别 | |
| description | VARCHAR(500) | 规则描述 | |
| is_enabled | INT | 是否启用 | 1-启用，0-禁用 |
| is_deleted | INT | 逻辑删除 | 0-未删除，1-已删除 |
| create_time | DATETIME | 创建时间 | |
| update_time | DATETIME | 更新时间 | |

#### 6.1.7 收藏夹表（favorite）

| 字段名 | 类型 | 说明 | 约束 |
|-------|------|------|------|
| id | BIGINT | 收藏ID | 主键，自增 |
| user_id | BIGINT | 用户ID | 外键 |
| data_type | VARCHAR(20) | 数据类型 | |
| data_id | BIGINT | 数据ID | |
| data_name | VARCHAR(100) | 数据名称 | |
| is_deleted | INT | 逻辑删除 | 0-未删除，1-已删除 |
| create_time | DATETIME | 创建时间 | |

#### 6.1.8 数据同步日志表（data_sync_log）

| 字段名 | 类型 | 说明 | 约束 |
|-------|------|------|------|
| id | BIGINT | 日志ID | 主键，自增 |
| sync_type | VARCHAR(20) | 同步类型 | |
| sync_status | VARCHAR(20) | 同步状态 | 成功/失败 |
| start_time | DATETIME | 开始时间 | |
| end_time | DATETIME | 结束时间 | |
| record_count | INT | 同步记录数 | |
| error_message | TEXT | 错误信息 | |
| operator_id | BIGINT | 操作人ID | |
| create_time | DATETIME | 创建时间 | |

### 6.2 Hive数据库设计

Hive主要用于大数据分析，数据结构通常与MySQL保持一致，但会根据分析需求进行优化。

#### 6.1.1 员工档案表（employee_profile）

```sql
CREATE EXTERNAL TABLE IF NOT EXISTS employee_profile (
    employee_no STRING COMMENT '员工工号',
    name STRING COMMENT '姓名',
    gender STRING COMMENT '性别',
    birth_date STRING COMMENT '出生日期',
    department_name STRING COMMENT '部门名称',
    position STRING COMMENT '职位',
    level STRING COMMENT '职级',
    hire_date STRING COMMENT '入职日期',
    status STRING COMMENT '在职状态',
    salary DECIMAL(10,2) COMMENT '薪资',
    work_years INT COMMENT '工作年限'
)
PARTITIONED BY (year STRING COMMENT '年份', month STRING COMMENT '月份')
STORED AS ORC
LOCATION '/hr/employee_profile';
```

#### 6.1.2 薪酬数据表（salary_data）

```sql
CREATE EXTERNAL TABLE IF NOT EXISTS salary_data (
    employee_no STRING COMMENT '员工工号',
    name STRING COMMENT '姓名',
    department_name STRING COMMENT '部门名称',
    base_salary DECIMAL(10,2) COMMENT '基本工资',
    performance_bonus DECIMAL(10,2) COMMENT '绩效奖金',
    welfare DECIMAL(10,2) COMMENT '福利',
    total_salary DECIMAL(10,2) COMMENT '总薪资',
    year INT COMMENT '年份',
    month INT COMMENT '月份'
)
PARTITIONED BY (year INT COMMENT '年份', month INT COMMENT '月份')
STORED AS ORC
LOCATION '/hr/salary_data';
```

---

## 7. API接口文档

### 7.1 认证接口

#### 7.1.1 用户登录

**接口地址**：POST /api/auth/login

**请求参数**：
```json
{
  "username": "admin",
  "password": "123456"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9...",
    "user": {
      "id": 1,
      "username": "admin",
      "name": "管理员",
      "role": "ADMIN"
    }
  }
}
```

#### 7.1.2 用户注册

**接口地址**：POST /api/auth/register

**请求参数**：
```json
{
  "username": "user001",
  "password": "123456",
  "name": "张三",
  "role": "USER",
  "deptId": 1,
  "deptName": "技术部",
  "phone": "13800138000",
  "email": "zhangsan@example.com"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

#### 7.1.3 获取用户信息

**接口地址**：GET /api/auth/info

**请求头**：
```
Authorization: Bearer eyJhbGciOiJIUzUxMiJ9...
```

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "admin",
    "name": "管理员",
    "role": "ADMIN",
    "deptName": "技术部"
  }
}
```

#### 7.1.4 修改密码

**接口地址**：POST /api/auth/change-password

**请求头**：
```
Authorization: Bearer eyJhbGciOiJIUzUxMiJ9...
```

**请求参数**：
```json
{
  "oldPassword": "123456",
  "newPassword": "654321"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 7.2 数据接口

#### 7.2.1 获取员工列表

**接口地址**：GET /api/data/employees

**请求参数**：
- page: 页码（默认1）
- size: 每页大小（默认10）
- keyword: 搜索关键词

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "employeeNo": "E001",
        "name": "张三",
        "departmentName": "技术部",
        "position": "工程师"
      }
    ],
    "total": 100,
    "size": 10,
    "current": 1
  }
}
```

#### 7.2.2 导入员工数据

**接口地址**：POST /api/data/employees/import

**请求头**：
```
Authorization: Bearer eyJhbGciOiJIUzUxMiJ9...
Content-Type: multipart/form-data
```

**请求参数**：
- file: Excel文件

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "successCount": 98,
    "failCount": 2,
    "errors": [
      {
        "row": 5,
        "message": "工号已存在"
      }
    ]
  }
}
```

### 7.3 分析接口

#### 7.3.1 薪酬福利分析

**接口地址**：GET /api/analysis/compensation

**请求参数**：
- year: 年份
- departmentId: 部门ID

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "averageSalary": 15000.00,
    "salaryDistribution": [
      {
        "range": "10000-15000",
        "count": 50
      },
      {
        "range": "15000-20000",
        "count": 30
      }
    ],
    "departmentComparison": [
      {
        "department": "技术部",
        "averageSalary": 18000.00
      }
    ]
  }
}
```

#### 7.3.2 员工流失分析

**接口地址**：GET /api/analysis/turnover

**请求参数**：
- year: 年份
- departmentId: 部门ID

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "turnoverRate": 0.05,
    "turnoverCount": 20,
    "turnoverByReason": [
      {
        "reason": "薪资",
        "count": 8
      },
      {
        "reason": "职业发展",
        "count": 7
      }
    ]
  }
}
```

### 7.4 仪表盘接口

#### 7.4.1 获取统计数据

**接口地址**：GET /api/dashboard/statistics

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "employeeCount": 1000,
    "departmentCount": 20,
    "newEmployeesThisMonth": 15,
    "turnoverRate": 0.03
  }
}
```

#### 7.4.2 获取预警信息

**接口地址**：GET /api/dashboard/warnings

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "warnings": [
      {
        "type": "高离职率",
        "message": "销售部本月离职率超过10%",
        "level": "high"
      }
    ]
  }
}
```

### 7.5 管理员接口

#### 7.5.1 数据同步

**接口地址**：POST /api/admin/sync

**请求头**：
```
Authorization: Bearer eyJhbGciOiJIUzUxMiJ9...
```

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "syncId": 1,
    "status": "running"
  }
}
```

#### 7.5.2 获取同步日志

**接口地址**：GET /api/admin/sync-logs

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "syncType": "FULL",
        "syncStatus": "SUCCESS",
        "recordCount": 1000,
        "startTime": "2026-03-01 10:00:00",
        "endTime": "2026-03-01 10:30:00"
      }
    ]
  }
}
```

---

## 8. 部署指南

### 8.1 环境要求

#### 8.1.1 后端环境

- JDK 1.8+
- Maven 3.8+
- MySQL 8.0+
- Hive 3.1.3+（可选，用于大数据分析）
- Tomcat 9.0+（或者使用Spring Boot内嵌Tomcat）

#### 8.1.2 前端环境

- Node.js 18+
- npm 9+ 或 yarn 1.22+

### 8.2 后端部署

#### 8.2.1 配置文件修改

1. **修改数据库配置**（application-prod.yml）：

```yaml
spring:
  datasource:
    url: jdbc:mysql://your-mysql-host:3306/hr_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: your-username
    password: your-password

  hive:
    url: jdbc:hive2://your-hive-host:10000/default
    username: your-hadoop-username
    password: your-hadoop-password
```

2. **修改JWT密钥**（application.yml）：

```yaml
jwt:
  secret: your-secret-key-at-least-256-bits-long
  expiration: 7200000
```

#### 8.2.2 编译打包

```bash
# 进入后端目录
cd backend

# 清理并打包
mvn clean package -DskipTests

# 生成的jar包位置：backend/target/hr-datacenter-1.0.0.jar
```

#### 8.2.3 运行应用

```bash
# 方式1：直接运行jar包
java -jar target/hr-datacenter-1.0.0.jar --spring.profiles.active=prod

# 方式2：后台运行
nohup java -jar target/hr-datacenter-1.0.0.jar --spring.profiles.active=prod > app.log 2>&1 &

# 方式3：使用systemd管理（Linux）
sudo vim /etc/systemd/system/hr-datacenter.service
```

systemd配置文件：

```ini
[Unit]
Description=HR Data Center Application
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/backend
ExecStart=/usr/bin/java -jar /path/to/backend/target/hr-datacenter-1.0.0.jar --spring.profiles.active=prod
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl start hr-datacenter
sudo systemctl enable hr-datacenter
sudo systemctl status hr-datacenter
```

### 8.3 前端部署

#### 8.3.1 安装依赖

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install
# 或
yarn install
```

#### 8.3.2 修改API地址

修改 `frontend/src/api` 目录下的各个文件，将API地址改为生产环境的地址：

```javascript
// 例如：frontend/src/api/auth.js
const baseURL = 'http://your-backend-host:8081/api'
```

#### 8.3.3 编译打包

```bash
# 开发环境运行
npm run dev

# 生产环境打包
npm run build

# 生成的静态文件在 frontend/dist 目录
```

#### 8.3.4 部署静态文件

将 `frontend/dist` 目录下的所有文件部署到Web服务器（如Nginx）：

Nginx配置示例：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # 反向代理后端API
    location /api {
        proxy_pass http://localhost:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启动Nginx：

```bash
sudo nginx -t
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 8.4 Docker部署（可选）

#### 8.4.1 后端Dockerfile

创建 `backend/Dockerfile`：

```dockerfile
FROM openjdk:8-jre-alpine

WORKDIR /app

COPY target/hr-datacenter-1.0.0.jar app.jar

EXPOSE 8081

ENTRYPOINT ["java", "-jar", "app.jar", "--spring.profiles.active=prod"]
```

构建镜像：

```bash
docker build -t hr-datacenter:1.0.0 .
```

运行容器：

```bash
docker run -d \
  --name hr-datacenter \
  -p 8081:8081 \
  -e SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/hr_db \
  -e SPRING_DATASOURCE_USERNAME=root \
  -e SPRING_DATASOURCE_PASSWORD=123456 \
  hr-datacenter:1.0.0
```

#### 8.4.2 前端Dockerfile

创建 `frontend/Dockerfile`：

```dockerfile
FROM node:18-alpine as builder

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html

COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

构建镜像：

```bash
docker build -t hr-datacenter-frontend:1.0.0 .
```

运行容器：

```bash
docker run -d \
  --name hr-datacenter-frontend \
  -p 80:80 \
  hr-datacenter-frontend:1.0.0
```

#### 8.4.3 Docker Compose

创建 `docker-compose.yml`：

```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: hr-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: hr_db
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./database/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql

  backend:
    build: ./backend
    container_name: hr-backend
    ports:
      - "8081:8081"
    depends_on:
      - mysql
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/hr_db
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: 123456

  frontend:
    build: ./frontend
    container_name: hr-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql-data:
```

启动所有服务：

```bash
docker-compose up -d
```

---

## 9. 开发规范

### 9.1 代码规范

#### 9.1.1 命名规范

- **类名**：使用大驼峰命名法（PascalCase），如 `UserService`
- **方法名**：使用小驼峰命名法（camelCase），如 `getUserById`
- **变量名**：使用小驼峰命名法，如 `userName`
- **常量名**：使用全大写下划线分隔，如 `MAX_PAGE_SIZE`
- **包名**：使用全小写，如 `com.hr.backend.service`

#### 9.1.2 注释规范

- **类注释**：必须包含类说明、作者、版本等信息
- **方法注释**：必须包含功能说明、参数说明、返回值说明
- **字段注释**：复杂字段必须添加注释
- **行内注释**：复杂逻辑必须添加行内注释

#### 9.1.3 代码格式

- 使用4空格缩进
- 每行代码不超过120字符
- 大括号不换行
- 运算符前后添加空格

### 9.2 Git规范

#### 9.2.1 分支管理

- **master**：主分支，用于生产环境
- **develop**：开发分支，用于集成开发
- **feature/***：功能分支，用于开发新功能
- **bugfix/***：修复分支，用于修复bug
- **hotfix/***：热修复分支，用于紧急修复

#### 9.2.2 提交规范

提交信息格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

Type类型：
- feat：新功能
- fix：修复bug
- docs：文档更新
- style：代码格式调整
- refactor：重构
- test：测试
- chore：构建过程或辅助工具的变动

示例：

```
feat(auth): 添加JWT认证功能

- 实现JWT Token生成
- 实现JWT Token验证
- 添加JWT认证过滤器

Closes #123
```

### 9.3 接口规范

#### 9.3.1 接口命名

- GET：获取资源，如 `/api/users`
- POST：创建资源，如 `/api/users`
- PUT：更新资源，如 `/api/users/{id}`
- DELETE：删除资源，如 `/api/users/{id}`

#### 9.3.2 响应格式

统一响应格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

#### 9.3.3 错误码规范

- 200：成功
- 400：参数错误
- 401：未授权
- 403：权限不足
- 404：资源不存在
- 500：系统错误

### 9.4 数据库规范

#### 9.4.1 表命名

- 使用小写下划线分隔，如 `user_profile`
- 表名使用复数形式，如 `users`

#### 9.4.2 字段命名

- 使用小写下划线分隔，如 `user_name`
- 必须包含主键 `id`
- 必须包含 `create_time` 和 `update_time`
- 建议使用逻辑删除字段 `is_deleted`

#### 9.4.3 索引规范

- 主键自动创建索引
- 外键字段建议创建索引
- 经常查询的字段建议创建索引
- 联合索引遵循最左前缀原则

---

## 附录

### A. 常见问题

#### A.1 启动时报错：循环依赖

**问题**：应用启动时报错 `BeanCurrentlyInCreationException`

**原因**：Bean之间存在循环依赖

**解决方案**：
1. 将 `PasswordEncoder` Bean从 `SecurityConfig` 中分离到 `PasswordEncoderConfig`
2. 或者在配置文件中添加 `spring.main.allow-circular-references=true`

#### A.2 启动时报错：AbstractMethodError

**问题**：应用启动时报错 `AbstractMethodError`

**原因**：Hive依赖与Tomcat版本不兼容

**解决方案**：已在 `TomcatConfig` 中禁用TLD扫描

#### A.3 Hive连接失败

**问题**：无法连接到Hive

**原因**：
1. Hive服务未启动
2. 网络不通
3. 用户名密码错误

**解决方案**：
1. 检查Hive服务是否启动
2. 检查网络连接
3. 检查配置文件中的连接信息

### B. 技术支持

如有问题，请联系：
- 邮箱：support@hrdatacenter.com
- 电话：400-XXX-XXXX
- 官网：http://www.hrdatacenter.com

### C. 更新日志

#### v1.0.0 (2026-03-03)

- 初始版本发布
- 实现用户认证与授权
- 实现员工档案管理
- 实现数据同步功能
- 实现数据分析功能
- 实现仪表盘功能
- 实现报表模板管理
- 实现预警规则管理
- 实现收藏夹功能

---

**文档结束**

© 2026 HrDataCenter开发团队. All rights reserved.
