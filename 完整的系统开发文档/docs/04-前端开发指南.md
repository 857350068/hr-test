# 人力资源数据中心系统 - 前端开发指南

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 前端开发指南 |
| 文档版本 | v1.0 |
| 创建日期 | 2026-03-03 |
| 文档状态 | 正式版 |

---

## 一、前端技术栈

### 1.1 核心框架

| 技术 | 版本 | 说明 |
|------|------|------|
| **Vue.js** | 3.4.0 | 渐进式JavaScript框架，使用Composition API |
| **Vite** | 5.0.0 | 下一代前端构建工具，提供极速的开发体验 |
| **TypeScript** | 5.x | JavaScript的超集，提供类型安全 |

### 1.2 UI组件库

| 技术 | 版本 | 说明 |
|------|------|------|
| **Element Plus** | 2.5.0 | Vue 3企业级UI组件库 |
| **ECharts** | 5.4.3 | 数据可视化图表库 |

### 1.3 状态管理与路由

| 技术 | 版本 | 说明 |
|------|------|------|
| **Pinia** | 2.1.7 | Vue 3官方推荐的状态管理库 |
| **Vue Router** | 4.2.5 | Vue.js官方路由管理器 |

### 1.4 工具库

| 技术 | 版本 | 说明 |
|------|------|------|
| **Axios** | 1.6.5 | HTTP客户端，用于API请求 |
| **Day.js** | latest | 轻量级日期处理库 |
| **Lodash-es** | latest | JavaScript实用工具库 |
| **Sass** | latest | CSS预处理器 |

---

## 二、项目结构

### 2.1 目录结构

```
frontend/
├── public/                 # 静态资源
│   ├── favicon.ico        # 网站图标
│   └── index.html         # HTML模板
├── src/                   # 源代码
│   ├── api/              # API接口
│   │   ├── auth.ts       # 认证接口
│   │   ├── user.ts       # 用户接口
│   │   ├── data.ts       # 数据接口
│   │   ├── analysis.ts   # 分析接口
│   │   ├── warning.ts    # 预警接口
│   │   └── report.ts     # 报表接口
│   ├── assets/           # 资源文件
│   │   ├── images/       # 图片
│   │   ├── styles/       # 全局样式
│   │   └── icons/        # 图标
│   ├── components/       # 公共组件
│   │   ├── common/       # 通用组件
│   │   │   ├── Header.vue       # 头部组件
│   │   │   ├── Sidebar.vue      # 侧边栏组件
│   │   │   └── Footer.vue       # 底部组件
│   │   ├── charts/       # 图表组件
│   │   │   ├── LineChart.vue    # 折线图
│   │   │   ├── BarChart.vue     # 柱状图
│   │   │   ├── PieChart.vue     # 饼图
│   │   │   └── RadarChart.vue   # 雷达图
│   │   └── business/    # 业务组件
│   │       ├── DataTable.vue    # 数据表格
│   │       ├── SearchForm.vue   # 搜索表单
│   │       └── StatCard.vue     # 统计卡片
│   ├── composables/      # 组合式函数
│   │   ├── useAuth.ts   # 认证逻辑
│   │   └── useTable.ts  # 表格逻辑
│   ├── layout/           # 布局组件
│   │   └── index.vue    # 主布局
│   ├── router/           # 路由配置
│   │   └── index.ts     # 路由定义
│   ├── stores/           # 状态管理
│   │   ├── user.ts      # 用户状态
│   │   ├── app.ts       # 应用状态
│   │   └── permission.ts # 权限状态
│   ├── utils/            # 工具函数
│   │   ├── request.ts   # 请求封装
│   │   ├── auth.ts      # 认证工具
│   │   ├── validate.ts  # 验证工具
│   │   └── format.ts    # 格式化工具
│   ├── views/            # 页面组件
│   │   ├── login/       # 登录页
│   │   ├── dashboard/   # 数据看板
│   │   ├── data/        # 数据管理
│   │   ├── analysis/    # 数据分析
│   │   ├── warning/     # 预警管理
│   │   ├── training/    # 培训管理
│   │   ├── performance/ # 绩效管理
│   │   ├── favorite/    # 收藏管理
│   │   ├── user/        # 用户管理
│   │   └── profile/     # 个人中心
│   ├── App.vue          # 根组件
│   └── main.ts          # 入口文件
├── .env.development     # 开发环境变量
├── .env.production      # 生产环境变量
├── .eslintrc.cjs        # ESLint配置
├── .prettierrc.json     # Prettier配置
├── index.html           # HTML模板
├── package.json         # 项目依赖
├── tsconfig.json        # TypeScript配置
├── vite.config.ts       # Vite配置
└── README.md            # 项目说明
```

### 2.2 核心文件说明

#### 2.2.1 package.json

```json
{
  "name": "hr-datacenter-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path .gitignore"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.5",
    "pinia": "^2.1.7",
    "element-plus": "^2.5.0",
    "axios": "^1.6.5",
    "echarts": "^5.4.3",
    "dayjs": "^1.11.10",
    "lodash-es": "^4.17.21",
    "@element-plus/icons-vue": "^2.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vue-tsc": "^1.8.0",
    "sass": "^1.69.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.0",
    "@types/node": "^20.10.0",
    "@types/lodash-es": "^4.17.12"
  }
}
```

#### 2.2.2 vite.config.ts

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  css: {
    preprocessorOptions: {
      scss: {
        additionalData: `@import "@/assets/styles/variables.scss";`
      }
    }
  },
  server: {
    port: 5173,
    open: true,
    proxy: {
      '/api': {
        target: 'http://localhost:8081',
        changeOrigin: true
      }
    }
  },
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'element-plus': ['element-plus'],
          'echarts': ['echarts']
        }
      }
    }
  }
})
```

---

## 三、核心配置

### 3.1 环境变量配置

#### 3.1.1 .env.development

```env
# 开发环境配置
VITE_APP_TITLE=人力资源数据中心系统
VITE_APP_API_BASE_URL=/api
VITE_APP_API_TIMEOUT=30000
VITE_APP_UPLOAD_URL=/api/upload
```

#### 3.1.2 .env.production

```env
# 生产环境配置
VITE_APP_TITLE=人力资源数据中心系统
VITE_APP_API_BASE_URL=/api
VITE_APP_API_TIMEOUT=30000
VITE_APP_UPLOAD_URL=/api/upload
```

### 3.2 TypeScript配置

```typescript
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

---

## 四、路由配置

### 4.1 路由定义

```typescript
import { createRouter, createWebHistory, RouteRecordRaw } from 'vue-router'
import { useUserStore } from '@/stores/user'
import { usePermissionStore } from '@/stores/permission'

const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/login/index.vue'),
    meta: { title: '登录', hidden: true }
  },
  {
    path: '/',
    component: () => import('@/layout/index.vue'),
    redirect: '/dashboard',
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('@/views/dashboard/index.vue'),
        meta: { title: '数据看板', icon: 'Dashboard' }
      },
      {
        path: 'data',
        name: 'Data',
        component: () => import('@/views/data/index.vue'),
        meta: { title: '数据管理', icon: 'Data' }
      },
      {
        path: 'analysis',
        name: 'Analysis',
        component: () => import('@/views/analysis/index.vue'),
        meta: { title: '数据分析', icon: 'Analysis' }
      },
      {
        path: 'warning',
        name: 'Warning',
        component: () => import('@/views/warning/index.vue'),
        meta: { title: '预警管理', icon: 'Warning' }
      },
      {
        path: 'training',
        name: 'Training',
        component: () => import('@/views/training/index.vue'),
        meta: { title: '培训管理', icon: 'Training' }
      },
      {
        path: 'performance',
        name: 'Performance',
        component: () => import('@/views/performance/index.vue'),
        meta: { title: '绩效管理', icon: 'Performance' }
      },
      {
        path: 'favorite',
        name: 'Favorite',
        component: () => import('@/views/favorite/index.vue'),
        meta: { title: '收藏管理', icon: 'Star' }
      },
      {
        path: 'user',
        name: 'User',
        component: () => import('@/views/user/index.vue'),
        meta: { title: '用户管理', icon: 'User', roles: ['ADMIN'] }
      },
      {
        path: 'profile',
        name: 'Profile',
        component: () => import('@/views/profile/index.vue'),
        meta: { title: '个人中心', icon: 'User', hidden: true }
      }
    ]
  },
  {
    path: '/:pathMatch(.*)*',
    redirect: '/dashboard'
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach(async (to, from, next) => {
  const userStore = useUserStore()
  const permissionStore = usePermissionStore()
  const token = userStore.token

  if (to.path === '/login') {
    if (token) {
      next({ path: '/' })
    } else {
      next()
    }
    return
  }

  if (!token) {
    next({ path: '/login', query: { redirect: to.fullPath } })
    return
  }

  // 检查权限
  if (to.meta.roles && !to.meta.roles.includes(userStore.role)) {
    next({ path: '/dashboard' })
    return
  }

  next()
})

export default router
```

---

## 五、状态管理

### 5.1 用户状态（User Store）

```typescript
import { defineStore } from 'pinia'
import { login, logout, getUserInfo } from '@/api/auth'
import { setToken, getToken, removeToken } from '@/utils/auth'

export const useUserStore = defineStore('user', {
  state: () => ({
    token: getToken() || '',
    userInfo: null as any,
    role: '' as string,
    permissions: [] as string[]
  }),

  getters: {
    isLoggedIn: (state) => !!state.token,
    userName: (state) => state.userInfo?.realName || '',
    avatar: (state) => state.userInfo?.avatar || ''
  },

  actions: {
    // 登录
    async login(loginForm: any) {
      const { data } = await login(loginForm)
      this.token = data.token
      setToken(data.token)
      return data
    },

    // 获取用户信息
    async getUserInfo() {
      const { data } = await getUserInfo()
      this.userInfo = data.userInfo
      this.role = data.userInfo.roleCode
      this.permissions = data.permissions
      return data
    },

    // 登出
    async logout() {
      await logout()
      this.token = ''
      this.userInfo = null
      this.role = ''
      this.permissions = []
      removeToken()
    }
  }
})
```

### 5.2 应用状态（App Store）

```typescript
import { defineStore } from 'pinia'

export const useAppStore = defineStore('app', {
  state: () => ({
    sidebar: {
      opened: true,
      withoutAnimation: false
    },
    device: 'desktop' as 'desktop' | 'mobile',
    size: 'default' as 'default' | 'large' | 'small',
    language: 'zh-cn'
  }),

  getters: {
    sidebarStatus: (state) => state.sidebar.opened
  },

  actions: {
    toggleSidebar() {
      this.sidebar.opened = !this.sidebar.opened
      this.sidebar.withoutAnimation = false
    },

    closeSidebar(withoutAnimation?: boolean) {
      this.sidebar.opened = false
      this.sidebar.withoutAnimation = withoutAnimation || false
    },

    toggleDevice(device: 'desktop' | 'mobile') {
      this.device = device
    },

    setSize(size: 'default' | 'large' | 'small') {
      this.size = size
    },

    setLanguage(language: string) {
      this.language = language
    }
  }
})
```

---

## 六、API接口封装

### 6.1 请求封装（utils/request.ts）

```typescript
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/stores/user'
import { getToken } from '@/utils/auth'

// 创建axios实例
const service: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_APP_API_BASE_URL,
  timeout: Number(import.meta.env.VITE_APP_API_TIMEOUT) || 30000
})

// 请求拦截器
service.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    const token = getToken()
    if (token && config.headers) {
      config.headers['Authorization'] = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    console.error('Request error:', error)
    return Promise.reject(error)
  }
)

// 响应拦截器
service.interceptors.response.use(
  (response: AxiosResponse) => {
    const res = response.data

    // 根据业务状态码处理
    if (res.code !== 200) {
      ElMessage.error(res.message || '请求失败')

      // 401: 未授权
      if (res.code === 401) {
        const userStore = useUserStore()
        userStore.logout()
        window.location.href = '/login'
      }

      return Promise.reject(new Error(res.message || '请求失败'))
    } else {
      return res
    }
  },
  (error) => {
    console.error('Response error:', error)
    ElMessage.error(error.message || '网络错误')
    return Promise.reject(error)
  }
)

export default service
```

### 6.2 API接口定义

#### 6.2.1 认证接口（api/auth.ts）

```typescript
import request from '@/utils/request'

export interface LoginForm {
  username: string
  password: string
  captcha?: string
}

export interface UserInfo {
  id: number
  username: string
  realName: string
  email: string
  avatar: string
  roleCode: string
  roleName: string
  permissions: string[]
}

// 登录
export const login = (data: LoginForm) => {
  return request({
    url: '/auth/login',
    method: 'post',
    data
  })
}

// 登出
export const logout = () => {
  return request({
    url: '/auth/logout',
    method: 'post'
  })
}

// 获取用户信息
export const getUserInfo = () => {
  return request({
    url: '/auth/userinfo',
    method: 'get'
  })
}

// 刷新Token
export const refreshToken = () => {
  return request({
    url: '/auth/refresh',
    method: 'post'
  })
}
```

#### 6.2.2 数据接口（api/data.ts）

```typescript
import request from '@/utils/request'

export interface QueryParams {
  pageNum?: number
  pageSize?: number
  categoryId?: number
  keyword?: string
  departmentId?: number
  positionId?: number
  startDate?: string
  endDate?: string
}

export interface DataItem {
  id: number
  categoryId: number
  categoryName: string
  dataDimension: string
  statPeriod: string
  indicatorValue: number
  trendRate: number
  chartData: any
  createTime: string
}

// 查询数据列表
export const getDataList = (params: QueryParams) => {
  return request({
    url: '/data/list',
    method: 'get',
    params
  })
}

// 查询数据详情
export const getDataDetail = (id: number) => {
  return request({
    url: `/data/${id}`,
    method: 'get'
  })
}

// 导出数据
export const exportData = (params: QueryParams) => {
  return request({
    url: '/data/export',
    method: 'get',
    params,
    responseType: 'blob'
  })
}

// 获取数据分类
export const getDataCategories = () => {
  return request({
    url: '/data/categories',
    method: 'get'
  })
}
```

#### 6.2.3 分析接口（api/analysis.ts）

```typescript
import request from '@/utils/request'

export interface AnalysisParams {
  type: string
  departmentId?: number
  startDate?: string
  endDate?: string
}

// 组织效能分析
export const analyzeOrgEfficiency = (params: AnalysisParams) => {
  return request({
    url: '/analysis/org-efficiency',
    method: 'get',
    params
  })
}

// 人才梯队分析
export const analyzeTalentPipeline = (params: AnalysisParams) => {
  return request({
    url: '/analysis/talent-pipeline',
    method: 'get',
    params
  })
}

// 薪酬福利分析
export const analyzeCompensation = (params: AnalysisParams) => {
  return request({
    url: '/analysis/compensation',
    method: 'get',
    params
  })
}

// 绩效管理分析
export const analyzePerformance = (params: AnalysisParams) => {
  return request({
    url: '/analysis/performance',
    method: 'get',
    params
  })
}

// 培训效果分析
export const analyzeTraining = (params: AnalysisParams) => {
  return request({
    url: '/analysis/training',
    method: 'get',
    params
  })
}
```

#### 6.2.4 预警接口（api/warning.ts）

```typescript
import request from '@/utils/request'

export interface WarningQueryParams {
  pageNum?: number
  pageSize?: number
  warningType?: string
  warningLevel?: number
  status?: number
  startDate?: string
  endDate?: string
}

export interface WarningItem {
  id: number
  warningType: string
  warningLevel: number
  warningContent: string
  warningObject: string
  objectId: number
  status: number
  createTime: string
}

// 查询预警列表
export const getWarningList = (params: WarningQueryParams) => {
  return request({
    url: '/warning/list',
    method: 'get',
    params
  })
}

// 查询预警详情
export const getWarningDetail = (id: number) => {
  return request({
    url: `/warning/${id}`,
    method: 'get'
  })
}

// 处理预警
export const handleWarning = (id: number, data: any) => {
  return request({
    url: `/warning/${id}/handle`,
    method: 'post',
    data
  })
}

// 获取预警统计
export const getWarningStatistics = () => {
  return request({
    url: '/warning/statistics',
    method: 'get'
  })
}
```

---

## 七、公共组件开发

### 7.1 统计卡片（StatCard.vue）

```vue
<template>
  <div class="stat-card">
    <div class="stat-card__icon" :style="{ backgroundColor: color }">
      <el-icon :size="24">
        <component :is="icon" />
      </el-icon>
    </div>
    <div class="stat-card__content">
      <div class="stat-card__title">{{ title }}</div>
      <div class="stat-card__value">{{ value }}</div>
      <div class="stat-card__trend" :class="trendClass">
        <el-icon><component :is="trendIcon" /></el-icon>
        <span>{{ trend }}</span>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { ArrowUp, ArrowDown } from '@element-plus/icons-vue'

interface Props {
  title: string
  value: string | number
  trend: string
  trendUp: boolean
  color: string
  icon: any
}

const props = withDefaults(defineProps<Props>(), {
  trendUp: true
})

const trendIcon = computed(() => props.trendUp ? ArrowUp : ArrowDown)
const trendClass = computed(() => props.trendUp ? 'trend-up' : 'trend-down')
</script>

<style scoped lang="scss">
.stat-card {
  display: flex;
  align-items: center;
  padding: 20px;
  background: #fff;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);

  &__icon {
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 12px;
    margin-right: 16px;
    color: #fff;
  }

  &__content {
    flex: 1;
  }

  &__title {
    font-size: 14px;
    color: #909399;
    margin-bottom: 8px;
  }

  &__value {
    font-size: 24px;
    font-weight: bold;
    color: #303133;
    margin-bottom: 8px;
  }

  &__trend {
    display: flex;
    align-items: center;
    font-size: 14px;

    &.trend-up {
      color: #67c23a;
    }

    &.trend-down {
      color: #f56c6c;
    }

    .el-icon {
      margin-right: 4px;
    }
  }
}
</style>
```

### 7.2 折线图组件（LineChart.vue）

```vue
<template>
  <div ref="chartRef" class="line-chart"></div>
</template>

<script setup lang="ts">
import { ref, onMounted, watch } from 'vue'
import * as echarts from 'echarts'
import type { EChartsOption } from 'echarts'

interface Props {
  data: {
    xAxis: string[]
    series: Array<{
      name: string
      data: number[]
      color?: string
    }>
  }
  height?: string
}

const props = withDefaults(defineProps<Props>(), {
  height: '400px'
})

const chartRef = ref<HTMLDivElement>()
let chartInstance: echarts.ECharts | null = null

const initChart = () => {
  if (!chartRef.value) return

  chartInstance = echarts.init(chartRef.value)
  updateChart()
}

const updateChart = () => {
  if (!chartInstance) return

  const option: EChartsOption = {
    tooltip: {
      trigger: 'axis',
      axisPointer: {
        type: 'cross'
      }
    },
    legend: {
      data: props.data.series.map((item) => item.name)
    },
    grid: {
      left: '3%',
      right: '4%',
      bottom: '3%',
      containLabel: true
    },
    xAxis: {
      type: 'category',
      boundaryGap: false,
      data: props.data.xAxis
    },
    yAxis: {
      type: 'value'
    },
    series: props.data.series.map((item) => ({
      name: item.name,
      type: 'line',
      smooth: true,
      data: item.data,
      itemStyle: {
        color: item.color
      },
      areaStyle: {
        opacity: 0.3
      }
    }))
  }

  chartInstance.setOption(option)
}

onMounted(() => {
  initChart()
  window.addEventListener('resize', () => {
    chartInstance?.resize()
  })
})

watch(
  () => props.data,
  () => {
    updateChart()
  },
  { deep: true }
)
</script>

<style scoped lang="scss">
.line-chart {
  width: 100%;
  height: v-bind(height);
}
</style>
```

### 7.3 数据表格组件（DataTable.vue）

```vue
<template>
  <div class="data-table">
    <el-table
      :data="tableData"
      v-loading="loading"
      stripe
      border
      @selection-change="handleSelectionChange"
    >
      <el-table-column v-if="selection" type="selection" width="55" />
      <el-table-column
        v-for="column in columns"
        :key="column.prop"
        :prop="column.prop"
        :label="column.label"
        :width="column.width"
        :min-width="column.minWidth"
        :fixed="column.fixed"
      >
        <template #default="{ row }">
          <slot v-if="column.slot" :name="column.slot" :row="row" />
          <span v-else>{{ row[column.prop] }}</span>
        </template>
      </el-table-column>
      <el-table-column v-if="actions" label="操作" :width="actionWidth" fixed="right">
        <template #default="{ row }">
          <el-button
            v-for="action in actions"
            :key="action.key"
            :type="action.type || 'primary'"
            :size="action.size || 'small'"
            :icon="action.icon"
            @click="action.handler(row)"
          >
            {{ action.label }}
          </el-button>
        </template>
      </el-table-column>
    </el-table>

    <el-pagination
      v-if="pagination"
      v-model:current-page="currentPage"
      v-model:page-size="pageSize"
      :page-sizes="[10, 20, 50, 100]"
      :total="total"
      layout="total, sizes, prev, pager, next, jumper"
      @size-change="handleSizeChange"
      @current-change="handleCurrentChange"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, watch } from 'vue'

interface Column {
  prop: string
  label: string
  width?: number
  minWidth?: number
  fixed?: boolean | 'left' | 'right'
  slot?: string
}

interface Action {
  key: string
  label: string
  type?: 'primary' | 'success' | 'warning' | 'danger' | 'info'
  size?: 'large' | 'default' | 'small'
  icon?: any
  handler: (row: any) => void
}

interface Props {
  data: any[]
  columns: Column[]
  loading?: boolean
  selection?: boolean
  actions?: Action[]
  pagination?: boolean
  total?: number
  actionWidth?: number
}

const props = withDefaults(defineProps<Props>(), {
  loading: false,
  selection: false,
  pagination: true,
  actionWidth: 200
})

const emit = defineEmits([
  'selection-change',
  'size-change',
  'current-change'
])

const tableData = ref(props.data)
const currentPage = ref(1)
const pageSize = ref(10)

watch(
  () => props.data,
  (val) => {
    tableData.value = val
  }
)

const handleSelectionChange = (selection: any[]) => {
  emit('selection-change', selection)
}

const handleSizeChange = (size: number) => {
  pageSize.value = size
  emit('size-change', size)
}

const handleCurrentChange = (page: number) => {
  currentPage.value = page
  emit('current-change', page)
}
</script>

<style scoped lang="scss">
.data-table {
  .el-pagination {
    margin-top: 20px;
    display: flex;
    justify-content: flex-end;
  }
}
</style>
```

---

## 八、页面开发示例

### 8.1 登录页面

```vue
<template>
  <div class="login-container">
    <div class="login-box">
      <div class="login-header">
        <h1>人力资源数据中心系统</h1>
        <p>HR Data Center System</p>
      </div>

      <el-form
        ref="loginFormRef"
        :model="loginForm"
        :rules="loginRules"
        class="login-form"
      >
        <el-form-item prop="username">
          <el-input
            v-model="loginForm.username"
            placeholder="请输入用户名"
            size="large"
            clearable
          >
            <template #prefix>
              <el-icon><User /></el-icon>
            </template>
          </el-input>
        </el-form-item>

        <el-form-item prop="password">
          <el-input
            v-model="loginForm.password"
            type="password"
            placeholder="请输入密码"
            size="large"
            show-password
            @keyup.enter="handleLogin"
          >
            <template #prefix>
              <el-icon><Lock /></el-icon>
            </template>
          </el-input>
        </el-form-item>

        <el-form-item>
          <el-button
            type="primary"
            size="large"
            :loading="loading"
            style="width: 100%"
            @click="handleLogin"
          >
            登录
          </el-button>
        </el-form-item>
      </el-form>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { useRouter, useRoute } from 'vue-router'
import { ElMessage } from 'element-plus'
import { User, Lock } from '@element-plus/icons-vue'
import { useUserStore } from '@/stores/user'
import type { FormInstance, FormRules } from 'element-plus'

const router = useRouter()
const route = useRoute()
const userStore = useUserStore()

const loginFormRef = ref<FormInstance>()
const loading = ref(false)

const loginForm = reactive({
  username: '',
  password: ''
})

const loginRules: FormRules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, message: '密码长度不能少于6位', trigger: 'blur' }
  ]
}

const handleLogin = async () => {
  if (!loginFormRef.value) return

  await loginFormRef.value.validate(async (valid) => {
    if (valid) {
      loading.value = true
      try {
        await userStore.login(loginForm)
        await userStore.getUserInfo()
        ElMessage.success('登录成功')
        const redirect = (route.query.redirect as string) || '/'
        router.push(redirect)
      } catch (error) {
        console.error('Login error:', error)
      } finally {
        loading.value = false
      }
    }
  })
}
</script>

<style scoped lang="scss">
.login-container {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.login-box {
  width: 420px;
  padding: 40px;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

.login-header {
  text-align: center;
  margin-bottom: 40px;

  h1 {
    font-size: 28px;
    color: #303133;
    margin-bottom: 8px;
  }

  p {
    font-size: 14px;
    color: #909399;
  }
}

.login-form {
  .el-form-item {
    margin-bottom: 24px;
  }
}
</style>
```

### 8.2 数据看板页面

```vue
<template>
  <div class="dashboard">
    <!-- 统计卡片 -->
    <el-row :gutter="20" class="stat-cards">
      <el-col :xs="24" :sm="12" :lg="6" v-for="stat in statistics" :key="stat.key">
        <stat-card
          :title="stat.title"
          :value="stat.value"
          :trend="stat.trend"
          :trend-up="stat.trendUp"
          :color="stat.color"
          :icon="stat.icon"
        />
      </el-col>
    </el-row>

    <!-- 图表区域 -->
    <el-row :gutter="20" class="charts">
      <el-col :xs="24" :lg="12">
        <el-card>
          <template #header>
            <span>组织效能趋势</span>
          </template>
          <line-chart :data="orgEfficiencyData" height="350px" />
        </el-card>
      </el-col>

      <el-col :xs="24" :lg="12">
        <el-card>
          <template #header>
            <span>人才梯队分布</span>
          </template>
          <pie-chart :data="talentPipelineData" height="350px" />
        </el-card>
      </el-col>
    </el-row>

    <!-- 预警信息 -->
    <el-card class="warning-section">
      <template #header>
        <div class="card-header">
          <span>预警信息</span>
          <el-button type="primary" size="small" @click="viewAllWarnings">
            查看全部
          </el-button>
        </div>
      </template>
      <data-table
        :data="warningList"
        :columns="warningColumns"
        :loading="loading"
        :actions="warningActions"
        :pagination="false"
      />
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import StatCard from '@/components/business/StatCard.vue'
import LineChart from '@/components/charts/LineChart.vue'
import PieChart from '@/components/charts/PieChart.vue'
import DataTable from '@/components/business/DataTable.vue'
import { getWarningList, getWarningStatistics } from '@/api/warning'
import { ElMessage } from 'element-plus'
import {
  User,
  TrendCharts,
  Warning,
  Money,
  View,
  Edit
} from '@element-plus/icons-vue'

const router = useRouter()
const loading = ref(false)

// 统计数据
const statistics = ref([
  {
    key: 'employees',
    title: '员工总数',
    value: '1,234',
    trend: '+5.2%',
    trendUp: true,
    color: '#409eff',
    icon: User
  },
  {
    key: 'efficiency',
    title: '组织效能',
    value: '88.5',
    trend: '+3.1%',
    trendUp: true,
    color: '#67c23a',
    icon: TrendCharts
  },
  {
    key: 'warnings',
    title: '预警数量',
    value: '12',
    trend: '-2.5%',
    trendUp: false,
    color: '#e6a23c',
    icon: Warning
  },
  {
    key: 'cost',
    title: '人力成本',
    value: '¥520万',
    trend: '+8.3%',
    trendUp: true,
    color: '#f56c6c',
    icon: Money
  }
])

// 图表数据
const orgEfficiencyData = ref({
  xAxis: ['1月', '2月', '3月', '4月', '5月', '6月'],
  series: [
    {
      name: '组织效能指数',
      data: [85, 87, 88, 86, 89, 90],
      color: '#409eff'
    }
  ]
})

const talentPipelineData = ref({
  series: [
    { name: 'P7级', value: 5 },
    { name: 'P6级', value: 15 },
    { name: 'P5级', value: 25 },
    { name: 'P4级', value: 35 }
  ]
})

// 预警数据
const warningList = ref([])
const warningColumns = [
  { prop: 'warningType', label: '预警类型' },
  { prop: 'warningLevel', label: '预警级别', slot: 'warningLevel' },
  { prop: 'warningContent', label: '预警内容' },
  { prop: 'createTime', label: '预警时间' }
]

const warningActions = [
  {
    key: 'view',
    label: '查看',
    type: 'primary',
    icon: View,
    handler: (row: any) => viewWarningDetail(row)
  },
  {
    key: 'handle',
    label: '处理',
    type: 'success',
    icon: Edit,
    handler: (row: any) => handleWarning(row)
  }
]

// 加载数据
const loadData = async () => {
  loading.value = true
  try {
    // 加载预警列表
    const { data } = await getWarningList({ pageNum: 1, pageSize: 5 })
    warningList.value = data.list || []
  } catch (error) {
    console.error('Load data error:', error)
  } finally {
    loading.value = false
  }
}

// 查看全部预警
const viewAllWarnings = () => {
  router.push('/warning')
}

// 查看预警详情
const viewWarningDetail = (row: any) => {
  console.log('View warning detail:', row)
}

// 处理预警
const handleWarning = (row: any) => {
  console.log('Handle warning:', row)
}

onMounted(() => {
  loadData()
})
</script>

<style scoped lang="scss">
.dashboard {
  padding: 20px;

  .stat-cards {
    margin-bottom: 20px;
  }

  .charts {
    margin-bottom: 20px;

    .el-card {
      height: 100%;
    }
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
}
</style>
```

---

## 九、样式规范

### 9.1 全局样式变量（assets/styles/variables.scss）

```scss
// 颜色
$primary-color: #409eff;
$success-color: #67c23a;
$warning-color: #e6a23c;
$danger-color: #f56c6c;
$info-color: #909399;

// 文字颜色
$text-primary: #303133;
$text-regular: #606266;
$text-secondary: #909399;
$text-placeholder: #c0c4cc;

// 边框颜色
$border-base: #dcdfe6;
$border-light: #e4e7ed;
$border-lighter: #ebeef5;
$border-extra-light: #f2f6fc;

// 背景颜色
$bg-color: #f5f7fa;
$bg-color-page: #ffffff;

// 字体
$font-family: 'Helvetica Neue', Helvetica, 'PingFang SC', 'Hiragino Sans GB',
  'Microsoft YaHei', Arial, sans-serif;

// 间距
$spacing-xs: 4px;
$spacing-sm: 8px;
$spacing-md: 16px;
$spacing-lg: 24px;
$spacing-xl: 32px;

// 圆角
$border-radius-sm: 4px;
$border-radius-base: 8px;
$border-radius-lg: 12px;

// 阴影
$box-shadow-base: 0 2px 8px rgba(0, 0, 0, 0.1);
$box-shadow-light: 0 2px 4px rgba(0, 0, 0, 0.08);
$box-shadow-lighter: 0 2px 2px rgba(0, 0, 0, 0.06);

// 过渡
$transition-base: all 0.3s cubic-bezier(0.645, 0.045, 0.355, 1);
$transition-fast: all 0.2s cubic-bezier(0.645, 0.045, 0.355, 1);

// z-index
$z-index-normal: 1;
$z-index-dropdown: 1000;
$z-index-sticky: 1020;
$z-index-fixed: 1030;
$z-index-modal-backdrop: 1040;
$z-index-modal: 1050;
$z-index-popover: 1060;
$z-index-tooltip: 1070;
```

### 9.2 全局样式（assets/styles/index.scss）

```scss
@import './variables.scss';

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html,
body {
  width: 100%;
  height: 100%;
  font-family: $font-family;
  font-size: 14px;
  color: $text-primary;
  background-color: $bg-color;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

#app {
  width: 100%;
  height: 100%;
}

// 滚动条样式
::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

::-webkit-scrollbar-thumb {
  background: $border-light;
  border-radius: 3px;

  &:hover {
    background: $border-base;
  }
}

::-webkit-scrollbar-track {
  background: transparent;
}

// 清除浮动
.clearfix::after {
  content: '';
  display: block;
  clear: both;
}

// 文字溢出
.text-ellipsis {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.text-ellipsis-2 {
  display: -webkit-box;
  overflow: hidden;
  text-overflow: ellipsis;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

// Flex布局
.flex {
  display: flex;
}

.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}

.flex-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.flex-around {
  display: flex;
  align-items: center;
  justify-content: space-around;
}
```

---

## 十、开发规范

### 10.1 命名规范

#### 10.1.1 文件命名

- 组件文件：PascalCase，如 `DataTable.vue`
- 工具文件：camelCase，如 `request.ts`
- 样式文件：kebab-case，如 `variables.scss`

#### 10.1.2 变量命名

- 常量：UPPER_SNAKE_CASE，如 `API_BASE_URL`
- 变量：camelCase，如 `userName`
- 组件：PascalCase，如 `DataTable`
- CSS类名：kebab-case，如 `stat-card`

### 10.2 代码规范

#### 10.2.1 Vue组件规范

```vue
<template>
  <!-- 模板 -->
</template>

<script setup lang="ts">
// 1. 导入
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'

// 2. Props和Emits
interface Props {
  title: string
}
const props = withDefaults(defineProps<Props>(), {})

const emit = defineEmits(['change'])

// 3. 响应式数据
const loading = ref(false)
const dataList = ref([])

// 4. 计算属性
const totalCount = computed(() => dataList.value.length)

// 5. 方法
const loadData = async () => {
  // ...
}

// 6. 生命周期
onMounted(() => {
  loadData()
})
</script>

<style scoped lang="scss">
// 样式
</style>
```

#### 10.2.2 TypeScript规范

```typescript
// 1. 使用接口定义类型
interface UserInfo {
  id: number
  name: string
  email: string
}

// 2. 使用类型别名
type UserRole = 'ADMIN' | 'USER' | 'GUEST'

// 3. 使用泛型
function getData<T>(url: string): Promise<T> {
  return request.get(url)
}

// 4. 使用可选参数
function createUser(name: string, age?: number): UserInfo {
  return { id: Date.now(), name, email: '' }
}
```

### 10.3 Git提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
perf: 性能优化
test: 测试相关
chore: 构建/工具链相关
```

示例：
```
feat(dashboard): 添加数据看板页面
fix(login): 修复登录密码错误问题
docs(readme): 更新项目说明文档
```

---

## 十一、构建与部署

### 11.1 开发环境

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问地址
http://localhost:5173
```

### 11.2 生产构建

```bash
# 构建
npm run build

# 输出目录
dist/
```

### 11.3 部署

```bash
# 将dist目录上传到服务器
scp -r dist/* root@192.168.168.100:/usr/share/nginx/html/hr/

# 重启Nginx
nginx -s reload
```

---

## 十二、常见问题

### 12.1 开发问题

**Q: Vite启动失败？**
A: 检查Node.js版本是否≥16，删除node_modules和package-lock.json，重新安装依赖。

**Q: 样式不生效？**
A: 检查scoped属性，确认选择器是否正确，检查样式优先级。

**Q: 路由跳转失败？**
A: 检查路由配置，确认路径是否正确，检查路由守卫逻辑。

### 12.2 构建问题

**Q: 构建报错？**
A: 检查TypeScript类型错误，使用`npm run build`查看详细错误信息。

**Q: 构建体积过大？**
A: 使用代码分割，配置路由懒加载，优化依赖包。

---

## 十三、总结

本文档详细描述了人力资源数据中心系统的前端开发指南，包括技术栈、项目结构、核心配置、路由配置、状态管理、API接口封装、公共组件开发、页面开发示例、样式规范、开发规范、构建部署等内容。

前端采用Vue 3 + Vite + Element Plus技术栈，使用TypeScript提供类型安全，使用Pinia进行状态管理，使用Vue Router进行路由管理。

项目结构清晰，代码规范统一，具有良好的可维护性和可扩展性。

开发人员应遵循本文档的规范进行开发，确保代码质量和项目一致性。
